# Ayla 直播：SRS 自定义配置
# 基于官方 conf/docker.conf + HTTPS（供手机跨设备访问直播）。
# 证书：mkcert 生成（./certs/cert.pem + key.pem），docker-compose 挂载到 ./conf/cert/。
# 改动生效：docker compose restart srs

listen              1935;
max_connections     1000;
srs_log_tank        console;
daemon              off;

# SRS HTTP API（后端 apps/live/srs.py 内部查询状态用，保持 127.0.0.1 默认无需 https）
http_api {
    enabled         on;
    listen          1985;
}

# 播放服务器：8080 = HTTP-FLV/HLS；8088 = HTTPS-FLV/HLS（手机 https 页面用）
http_server {
    enabled         on;
    listen          8080;
    dir             ./objs/nginx/html;
    https {
        enabled     on;
        listen      8088;
        key         ./conf/cert/key.pem;
        cert        ./conf/cert/cert.pem;
    }
}

rtc_server {
    enabled on;
    listen 8000; # UDP port
    # @see https://ossrs.net/lts/zh-cn/docs/v4/doc/webrtc#config-candidate
    candidate $CANDIDATE;
}

vhost __defaultVhost__ {
    hls {
        enabled         on;
    }
    http_remux {
        enabled     on;
        mount       [vhost]/[app]/[stream].flv;
    }
    rtc {
        enabled     on;
        # @see https://ossrs.net/lts/zh-cn/docs/v4/doc/webrtc#rtmp-to-rtc
        rtmp_to_rtc on;
        # @see https://ossrs.net/lts/zh-cn/docs/v4/doc/webrtc#rtc-to-rtmp
        rtc_to_rtmp on;
    }
}
