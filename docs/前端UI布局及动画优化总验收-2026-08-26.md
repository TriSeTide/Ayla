# Ayla 前端 UI 布局及动画优化总验收

> 执行日期：2026-08-28
> 覆盖范围：2026-08-25 方案定稿 + 2026-08-26 起 M1–M6c 实施波次 + 本收尾对话（Z 规范同步 / 全量回归）
> 结论：**暂不验收（工程回归已绿，真机手感与真实登录态仍待用户确认）**

## 1. 结论摘要

- `Ayla/docs/design.md` 已按各分支交付报告审慎增补：§4 新配方、§7 framer-motion 纪律与 PageTransition 时长表、§12.7/§12.12 现行边界、§12.18–§12.20 新增配方。已撤销方案（竖屏弹幕浮层抽屉、`LivePeerPreview` 三槽预览卡）以「方案留档 + 当前实现边界」记录，不伪装成现行实现。
- 工程回归：`npx --no-install tsc -b --noEmit` 通过；全量 Vitest **101 文件 / 705 项**通过；`npx --no-install vite build` 通过（645 modules，7.55s；仅既有 chunk / 动态导入 warning）。
- Playwright 双形态（375×667 / 1440×900）共 28 张关键页面截图已走查：无横向溢出、无 `pageerror`。走查使用独立 headless 浏览器 + REST fixture，不占用共享 MCP 浏览器，也未改仓库服务。
- 本轮为对齐 §12.12「查看器横滑全落地」补修了 `ImageViewer` 松手判定（接入 `useSwipeCommit`），并新增 3 项契约测试。

## 2. 波次任务清单

依据 `Ayla/docs/plans/前端UI布局及动画优化实施方案-2026-08-25.md` 与 `.workbuddy/memory/2026-08-25.md` / `2026-08-26.md`。状态列只区分：已提交 / 本对话已同步规范 / 已撤销 / 待真机。

| 波次 | 任务 | 提交 / 证据 | 状态 |
|---|---|---|---|
| 方案 | 实施方案 + 功能层问题登记 | `ea01a66` | 已提交 |
| M1a | framer-motion + PageTransition + BottomTabs 过渡 + 顶栏抽取 | `921e3e4` | 已提交 |
| M1b | 群内横滑跟手 / 下拉返回协同 / 进群编排 | `21aa65b` | 已提交 |
| 修复 | 语音房进房底栏未下滑 | `81962c4` | 已提交 |
| M2a | 一级五页跟手横滑 | `3a71316` | 已提交 |
| M2b | 图片查看器多媒体横滑 | `6b0c62a` + 本对话补修 `ImageViewer` | 代码已落地；本对话补齐 `useSwipeCommit` |
| M2c | 私信左边缘侧滑返回 | `7e387d2` | 已提交；用户 08-26 真机验收通过 |
| M2d | 直播间上下滑切台 | `9c9040a` + `6413dc8` | 已提交；现行为视频+弹幕区整体滑动 |
| M3a | PullToRefresh + reveal + useScrollRestore | `32006d5` | 已提交 |
| M3.4 | CornerFabStack / 刷新动画 / 列表浮入 | `3ee1657` | 已提交 |
| M4a | 爱莉专属气泡 + `.msg-arrive` | `35a122e` | 已提交 |
| M4b | 帖子流双列瀑布流 + 滚动恢复 | `66cc7bf` + `5b3cdfe` + `43a6fa6` | 已提交 |
| M4 直播收尾 | U3/U4/U6/U9；U5 浮层撤销 | `71f161c` | 已提交；U5 浮层/抽屉已撤销 |
| U15 | `.group-scene-head` 独立玻璃卡 | `43a6fa6` | 已提交 |
| M6 / U16 | 聊天窗口化 + 回到底部钮 | `22ef415` | 已提交 |
| F8/F9/F10 | @Token / @我跳转 / 未读定位 | `d2bf087` + `c54cd92` | 已提交 |
| Z | design.md 同步 + 全量回归 | 本提交：规范同步 + ImageViewer 松手判定对齐 | 工程回归已绿，真机待确认 |

## 3. design.md 增补清单

事实源：`Ayla/docs/design.md`。新增条目引用 `tokens.css` 变量，未引入裸 hex。

| 位置 | 内容 |
|---|---|
| §4 | Switch / Toggle 胶囊开关 |
| §4 | CornerFabStack 右下浮层按钮组 |
| §4 | `.group-scene-head` 群内标题栏 |
| §4 | 聊天「回到底部」钮 `.message-jump-bottom` |
| §7 | framer-motion 使用纪律（跟手 / 进出协同 / 编排；CSS keyframes 保留） |
| §7 | PageTransition 时长表：进 200ms 浮入 `y 20px` / 退 150ms 淡出；搜索页反向展开；群页/直播切台不叠加 |
| §7 | `.msg-arrive` 仅新到达消息；滚动恢复禁 stagger |
| §12.7 | 竖屏弹幕浮层化配方（U5 方案留档）+ **当前实现边界**（已撤销抽屉，现行透明弹幕区在视频下方） |
| §12.7.1 | `LivePeerPreview` 轻量预览配方（方案留档；组件已删除，现行不渲染预览卡） |
| §12.12 | 手势实现状态表：一级横滑 / 群内横滑 / 直播上下滑 / 查看器横滑 / 私信侧滑 / 下拉返回 / 下拉刷新均已落地 |
| §12.12 | 松手契约：`useSwipeCommit` = 净位移 ≥1/3 + 同向甩动 ≥300px/s 且 ≥40px + 方向锁让位；`pointercancel` 不判定 |
| §12.18 | 帖子流双列瀑布流（>1024px） |
| §12.19 | PullToRefresh 指示器 |
| §12.20 | `useScrollRestore`（全站帖子流 / 群内帖子 / 直播列表 / 我的帖子） |

## 4. 全量回归结果

| 项 | 命令 / 方法 | 结果 |
|---|---|---|
| Typecheck | `npx --no-install tsc -b --noEmit`（`Ayla/web`） | 通过 |
| Vitest | `NODE_OPTIONS=--max-old-space-size=4096 npx --no-install vitest run --maxWorkers=2 --minWorkers=1` | **101 passed / 705 passed** |
| 查看器定向 | `vitest run src/vitest/image-viewer-swipe.test.tsx` | 3/3 通过 |
| Build | `npx --no-install vite build` | 645 modules，7.55s；既有 chunk >500kB / 动态导入 warning |
| Playwright | 独立 headless Chromium，375 与 1440 各 14 路由 | 28 张截图；无横向溢出；无 `pageerror` |

Playwright 路由：`/group`、群内五场景、`/live`、`/live/1`、`/posts`、`/posts/mine`、`/messages`、`/chat/c1`、`/search?q=樱`、`/profile`。截图目录：`E:\dsh\ayla-z-regression\`。

走查约束（必须写明，避免误判）：

- 未写入账号密码；REST 走 fixture，WebSocket 未接入，页面底部「实时连接重连中」属走查环境提示，不是布局回归。
- 宽屏 `/group` 会落到最近群（fixture 为 `/group/42`），与产品「宽屏主页 = 最近群聊三栏」一致。
- 控制台仅见既有 `validateDOMNesting`（`PostCard` / `ConversationList` / 搜索结果里 `<button>` 嵌套 `Avatar` 的 `<button>`），对应功能层登记 F3，本轮不修。

## 5. 本对话代码边界

只新增/修改本收尾所需文件；并行对话的帖子详情改动未纳入本任务。

| 文件 | 归属 |
|---|---|
| `Ayla/docs/design.md` | 本对话：Z 规范同步 |
| `Ayla/web/src/components/chat/ImageViewer.tsx` | 本对话：松手判定对齐 `useSwipeCommit` |
| `Ayla/web/src/vitest/image-viewer-swipe.test.tsx` | 本对话：3 项契约测试 |
| `Ayla/web/src/pages/PostDetailPage.tsx` | **并行对话**，未改 |
| `Ayla/web/src/vitest/post-detail-page.test.tsx` | **并行对话**，未改 |
| `Ayla/web/src/vitest/post-detail-back-navigation.test.tsx` | **并行对话**，未改 |

## 6. 待用户决定

1. 真机复测：窄屏横滑/上下滑手感、键盘弹出、帖子详情返回滚动、直播弹幕滚动与切台边界。
2. 既有 F3（按钮嵌套）是否另开任务。
3. 探针/验证账号是否清理：`ui_probe_tmp`、`ptr_verify_tmp`、`scroll_verify_mta5w3s1` 等（见 08-25/08-26 日志）。
4. 本提交未 push；是否推送由用户决定。
