# Ayla Flutter 复刻 —— 新对话启动提示词

> **用法**：把本文件**全文**贴进新对话的第一条消息。
> **生成时间** 2026-09-21，对应 HEAD：子仓库 Ayla `0107577` / 父仓库 `884eab24`。
> **配套**：进度与待办见 `13-工作进度与待办.md`（**开工前必读**）；两份都要读。

---

## 一、你的任务（先读完本节再动手）

### 1.1 项目是什么

把 `Ayla/web`（React 18 + Vite + TS）**一比一复刻**到 `Ayla/flutter`
（Flutter 3.47.4 / Dart 3.13.3）。这是**纯前端复刻**工作 —— 后端已存在，你不动后端。

### 1.2 现在处于哪个阶段（**这一段最容易搞错，务必看清**）

```
✅ M0 基座（网络/状态/路由/WS）
✅ Batch 1–4  组件库基元（玻璃/按钮/卡片/目录筛选/输入）
✅ Batch 5    业务组件族（帖子卡/编辑器/评论/查看器/分享/媒体能力层）
🔄 Shell 层   底栏 ✅ / 群顶栏 ✅ / 响应式顶栏 ✅  → 【侧栏族 + 其它壳件 = 你现在的任务】
⏸ 页面层      一个都还没做（用户明确要求「后面统一做」）
```

### 1.3 你现在要做的事：**把组件库补全**

**这不是只剩几件——组件库还差一大批。** 对照 web 盘点（2026-09-21）：

```
web 端：11 个组件域 + 21 个顶层散件 + 14 个 layout 件
Flutter：现有 64 个公开组件类，5 处已完成（posts / share / home 大部分 / group 顶栏 / layout 3 件）
```

未开工的部分分四档（**完整清单 + 每件的 web 事实源见 `13-工作进度与待办.md` §二**）：

| 档 | 内容 | 量级 |
|---|---|---|
| **A. Shell 层剩余** | `ServerRail` / `ChannelSidebar`（627 行）/ `CreateSheet` / `CornerFabStack` 等 / `SessionActivityIndicator` | 6 项 |
| **B. 组件域** | voice（8）/ **live（16，含弹幕三件）** / **chat（12）** / boardgame（3）/ group 剩余（2）/ 顶层散件（4） | **约 45 个组件** |
| **C. 全库统一项** | `Tooltip` 悬停提示（全库零命中）、`MediaSigner` 修复 | 2 项，随手补 |
| **D. 页面层** | 全部页面 + `AppShell` + 路由 + motion 转场 | **最后统一做，现在一件都不许写** |

**工作节奏**：一次做一件（或一域一批），**做完等用户验收再开下一件**。
不要想着一口气做完 —— 用户会逐条指出不符项，改到符合为止。

**工作流**（每件都走一遍，不许跳步）：
```
① 按 §2.4 读全 web 源码链（含 auroraqua 全部命中与其 @media 归属）
② 按 §2.3 清点组件库能复用哪些（先查 14-组件库复用索引.md 的范本表）
③ 列出该件的「字段/尺寸/颜色/交互」推导表 + 复用清单 → 给用户过目
④ 用户点头后才写代码
⑤ analyze + 全量测试 + @Preview + 加进 component_gallery.dart
⑥ 汇报（改了什么/依据哪行/测试数）→ 等用户验收
⑦ 用户说 commit 才 commit（子仓库精确暂存 → 父仓库同步指针 + 子提交全文）
```

### 1.4 明确禁止（这几条都是本项目真实事故，别重犯）

| 禁止 | 为什么 |
|---|---|
| **禁止写页面**（`lib/pages/**`、路由、页面级编排） | 用户明确要求「页面是后面统一做的」。本会话我提前做了帖子流页面（`posts_hub_page.dart` 等），被要求删掉（revert `639eefc`）。**Shell 件是壳层组件，不是页面**，别搞混 |
| **禁止先简化/先占位/后续再补** | 已因此发生过事故 |
| **禁止凭经验写任何数值**（尺寸/颜色/间距/圆角/阴影/文案/图标） | 必须能答出「对应 web 哪个文件哪一行」 |
| **禁止留 `TODO(待补)` 就交付** | 用户原话「怎么老是漏东西」；要么当轮补，要么明确标为未完成 |
| **禁止从 CSS 重新推导已有范本的交互** | 用户原话「这是已经有组件做好了范本的，学」；先查 `14-组件库复用索引.md` |
| **禁止自作主张 commit / build / 启预览** | 只有用户说「commit」「构建」才做；预览是用户的窗口 |
| **禁止提交 M0 遗留文件** | 清单见 §六 |

### 1.5 什么算「这一件做完了」

- `dart analyze lib` **零告警**；
- `flutter test --concurrency 1` **全绿**（当前基线 **208/208**）；
- 有 `@Preview` 且已加入 `lib/preview/component_gallery.dart`（**无预览 = 未交付**）；
- 有定向测试（至少覆盖：结构 / 关键尺寸 / 交互回调 / 易错点断言）；
- 汇报里给出**逐条「web 行号 ↔ 实现」对照**；
- 用户验收通过。

### 1.6 开工顺序（先别写代码）

1. 完整阅读 `/mnt/e/Elysium-AyerElysia/Elysium/AGENTS.md`（项目最高约束）
2. 读 `.workbuddy/memory/MEMORY.md`（项目级长期记忆）
3. 加载两个 skill：`ayla-flutter-development` + `ayla-frontend-design`
4. 读 `Ayla/docs/flutter/13-工作进度与待办.md`（**做到哪了、下一件是什么**）
5. 读 `Ayla/docs/flutter/14-组件库复用索引.md`（**先查范本，别重新发明**）
6. 读本文 §二–§五（约束 / 环境 / 语义差 14 条 / 已踩的坑）
7. `git -C Ayla log --oneline -8` + `git -C Ayla status --short` 确认现状
8. 从 §1.3 的第 1 件开始（`AylaServerRail`），按 §1.3 的工作流走

---
---

## 二、硬性约束（违反即返工）

### 2.1 项目红线（来自 AGENTS.md）
- **主体文件主权**：`SOUL.md` / `USER.md` / 爱莉的 `MEMORY.md` / 日记等第一人称语义
  **只能由爱莉本人写入**；你只能读、审计、建议，**绝不代笔**。
- **认知零规则**：代码不替主体裁决意义/真相/价值/学习内容/行动选择。
- **git 纪律**：
  - **禁止 `git stash`** 做临时验证/基线对比（用只读方式）
  - **Ayla 子仓库禁止 `gc`/`repack`/`prune`**（曾损坏对象库）
  - 脏工作树**禁止 `git add -A` / `git add .`**，只精确暂存本任务文件
  - 涉及启动的变更**由用户手动启动验收**，agent 不得代启 Elysium
  - **无用户明确命令不得 commit**；用户说「构建」就只构建，别顺手提交
- **Ayla 产生 commit 后父仓库必须同步子模块指针**，
  且父仓库提交消息**一字不漏包含子仓库 commit 全文**。

### 2.2 复刻黄金规则（用户定稿）
- **所有元素必须从现有 web 代码/样式推导**（`Ayla/web/src/**` + `Ayla/docs/design.md`）。
  **禁止**凭经验生成任何数值、颜色、尺寸、间距、圆角、阴影、文案、图标。
  推导不出来 → **停下来问用户**。
- **宽屏元素 ×1.25**（用户 Windows 缩放 125%，浏览器 DPR=1.25）。
  验收以「浏览器 125% 缩放下的 web」为基准。
- **禁止「先简化/先占位/后续再补」**（本项目已因此发生过事故）。
- **每个组件必须有 `@Preview`，并同时加入组件画布** —— **无预览 = 未交付**。

### 2.3 复用优先（用户三次点名，最容易被违反）
> **遇到可复用的内容不要手动自己写，要先看整个组件库；
> 只有在整个组件库里都没有实现的内容，才新增组件。**

- 历史事故：`privacy_sheet`/`dialogs`/`profile_and_filters` 里自造过按钮、输入框、卡面，
  被点名后全部换回组件库；顶栏模块项又差点从 CSS 重新推导一套。
- **同类交互先找库内范本**（用户 2026-09-20 原话：「这是已经有组件做好了范本的，学」）：
  选项卡/共享胶囊/扫光/按压 的范本 = **`AylaDirectoryFilters`**（`widgets/profile_and_filters.dart`）；
  菜单项 hover 的范本 = `conversation_more_menu.dart` 的 `_MenuItem`；
  按钮扫光的范本 = `GlassButton`（`theme/glass.dart`）；入场动画 = `AylaRevealItem`（`widgets/reveal.dart`）。
- 既有组件「看起来像但不完全一样」时：**先想能不能加档位，再想新建**，
  并在注释里写明「当前用哪档 + 另一档为何存在」。

### 2.4 改之前必须完整读 web 源码链（最惨痛教训）
用户原话：**「你改之前又没看web端代码」**。筛选条一个组件曾连续返工 10+ 轮，
根因**全部是「用推理代替读代码」**。

**强制顺序**（写任何视觉/交互代码前）：
1. `grep -rn '\.<类名>' styles/*.css` 出**全部**命中 —— 不只看你以为那个文件；
2. **逐条确认每条命中所属的 `@media` / 容器限定 / `:is()` 条件**；
3. 读 tsx：JSX 结构、`className` 拼接条件、事件绑定、hover state；
4. 读 `components/motion/auroraquaMotion.ts`（动画参数）；
5. 清点组件库能复用哪些；**才开始写代码**。

**`auroraqua.css` 专项**（它最后加载，覆盖前面同特异性规则，本会话栽过两次）：
它有 14 个 `@media`（行号 38 / 72 / 134 / 161 / 293 / 412 / 457 / 476 / 583 / 593 / 614 / 631 / 655），
其中 **412 与 583 是 `max-width: 768px`** —— **窄屏专属组件（底栏/群顶栏/窄屏顶栏）
一律以窄屏媒体查询的值为最终值**。例：`.bottom-tabs` 顶层圆角是 `20 20 0 0`，
但 447–448 把底栏与群顶栏一起清零 ⇒ 实际是**方角**。

**自检**：写下任何一行前问「这对应 web 的哪个文件哪一行？」答不出就是自由发挥。

### 2.5 不要留下未完成项（用户点名）
用户原话：**「怎么老是漏东西，仔细审查还有没有漏的全都修好」**。
- **禁止留 `TODO(待补)` 就交付**：要么当轮补齐，要么在汇报里明确列为「未完成」交用户裁决；
- **不要凭印象关闭组件库默认样式**（本会话连续踩坑）：我曾在底栏、群顶栏、顶栏三处凭空传
  `showBorder: false`，把 `.auroraqua-nav-highlight` 的 1px 白边全关了（`auroraqua.css:186`）；
  也曾在模块项写死 `height: 1.2`，破坏行高继承。**改动前先读该参数的库内默认值与 web 规定**。
- 交付前**逐条**做一遍「web 规则 ↔ 实现」对照审查，并把结论写进汇报。

---

## 三、环境与命令（实测可用）

- 工作区根：`/mnt/e/Elysium-AyerElysia/Elysium`（WSL2，Windows 盘 `/mnt/e`）
- Flutter：**3.47.4 / Dart 3.13.3**，在 Windows 侧 `E:\flutter-3.47.4`
- **调用方式**（WSL 不能直跑 `.bat`）：
  ```bash
  cmd.exe /c "cd /d E:\Elysium-AyerElysia\Elysium\Ayla\flutter && E:\flutter-3.47.4\bin\flutter.bat <args>"
  # 分析：
  cmd.exe /c "cd /d ...\flutter && E:\flutter-3.47.4\bin\dart.bat analyze lib"
  ```
  ⚠️ 多条 Windows 命令要写在**同一对引号内**用 `&&` 连接；写到引号外会被 bash 吃掉反斜杠。
- **测试必须串行**：`flutter test --concurrency 1`
  （并发会因内存压力导致 `icons_golden_test` "did not complete"）
- **预览**：`bash tool/preview-up.sh`（幂等；健康则复用）。
  ⚠️ **预览是用户的窗口**：agent **不得自行启动/重启预览、不得截图验收**（除非用户明确要求）。
  画布样张是模型唯一能「写」的验收面；视觉确认交给用户。
- **组件画布**：`lib/preview/component_gallery.dart`（每个新组件都要加一节）
- Windows 构建：`flutter build windows --release`
  → `build/windows/x64/runner/Release/ayla_flutter.exe`

---

## 四、Flutter ↔ CSS 语义差对照表（高频事故源，逐条防范）

| # | CSS 语义 | Flutter 实际行为 | 修法 |
|---|---|---|---|
| 1 | `box-shadow` **不在 border-box 内部绘制** | `BoxShadow` **铺满形状含内部** → 半透明面发灰、hover 时颜色染进按钮内部 | `AylaGlassShadow.ring`（挖空内部） |
| 2 | `background` 与 `border` **分离绘制** | `BoxDecoration` 同给 `gradient`+`border` 时**渐变盖住 1px 边框** | 拆层：底色 → 内高光 → **边框（独立层）** |
| 3 | `background-color` 过渡用 **premultiplied alpha** | `Color.lerp` **逐通道直插** → 从 `Colors.transparent`（**透明黑**）插值中途是**中性灰** | 零透明用**同色相** `color.withValues(alpha: 0)` |
| 4 | `overflow` 裁剪边界是 **padding box** | `SingleChildScrollView` 裁在**自己 content box** → 外阴影被裁断 | padding 放进滚动视图**内部** |
| 5 | `transition` 在**挂载时 `:hover` 已匹配** → 首帧直接到终点值，**不产生过渡** | `forward()` 从头播 → 行程被提前消耗 | 用 `setSweep(true, jump: true)` / `controller.value = 1.0` |
| 6 | `linear-gradient` 长度 `\|W·sinθ\|+\|H·cosθ\|` | `Alignment` 端点在**归一化空间**插值（只有正方形等价） | `cssLinearGradient(aspectRatio: 宽/高)`，在 `LayoutBuilder` 内生成 |
| 7 | `backdrop-filter: blur(24) saturate(1.4)` | 需 `ImageFilter.compose` | `GlassConfig.backdropFilter`；**saturate 分档**（24/18→1.4；8→无） |
| 8 | 7 个 `--glass-*` 阴影 token 全含 `--glass-inset` | `BoxShadow` 无 inset 变体 | `AylaInset.topHighlight(height)` / `AylaGlassInset.over` |
| 9 | `:hover` 是**状态谓词**（每帧求值） | `MouseRegion.onEnter/onExit` **只在指针移动时**触发 | 记录索引，在 **build 时求值** |
| 10 | `Stack` 越界装饰可见 | Flutter `Stack` 默认 `Clip.hardEdge` → **完全不可见**（但 `getRect` 报位置正确） | `clipBehavior: Clip.none` |
| 11 | 百分比位移的基准是**元素自身尺寸** | `AnimatedSlide`/`FractionallySizedBox` 基准是**直接 child** 尺寸 | 需精确像素时用 `TweenAnimationBuilder` + `Transform.translate` |
| 12 | CSS 负 `margin`（如 `margin-top: -8px`）= **上浮且占位减少** | `Transform.translate` 只做**视觉位移**、布局仍占满 → 撑破父容器 | `SizedBox(高-位移)` + `OverflowBox(alignment: bottomCenter)` |
| 13 | 无祖先 `DefaultTextStyle` 时用调试兜底 | **`OverlayEntry` 是独立子树**，页面 `Material`/`DefaultTextStyle` 传不进去 → `Text` 落到 `DefaultTextStyle.fallback`（**双下划线 + 红色**，用户称「黄线」） | 浮层一律走 `aylaOverlayEntry`（`widgets/overlays.dart`） |
| 14 | SVG 用 `currentColor` 继承 `body` 颜色 | `AylaIcon` 兜底链「显式 color → 祖先 `IconTheme` → `textPrimary`」，而 SDK 的 `ThemeData` 缺省会注入 **`IconThemeData(color: Color(0xDD000000))`**（肉眼即纯黑） | 主题必须显式设 `iconTheme`（`app_theme.dart` 已设 `textPrimary`） |

---

## 五、其他已踩过的坑（避免重复调查）

### 工具与流程
- **大段改写禁用字符串区间替换**：用 `edit` 工具精确替换；**禁止用 python 脚本替换目标文件**
  （用户明令：「别这样替换容易出错，用edit工具」）。脚本只用于**新建**文件或分片拼接。
- **`edit` 失败后必须立刻重新 `analyze`**：本会话因为「一条链里前一个 edit 失败、后一个成功」
  导致缺 import 漏到预览编译失败（`conversation_more_menu.dart` 的 `aylaOverlayEntry`）。
- **`grep` 结果要分清「注释」和「代码」**：我两次把注释里提到的 `height: 1.2` / `showBorder: false`
  当成实际代码，白跑一轮。
- **定位类问题先实测再改**：胶囊 64px、菜单落到 `(-16,151)`、垂直居中 116.0 都是**测出来**的；
  本会话用 `print('MEASURED …')` + `tester.getRect` 定位，比猜快得多。
- ⚠️ **`print` 里的 `${...}` 别转义**：写 `'x=${v}'` 而不是 `'x=\${v}'`（后者打印字面量）。
- **`/mnt/e`（drvfs）偶发 `EACCES`**：edit 的原子重命名会失败 → **直接重试**即可，文件不会损坏。

### Flutter / Dart 行为
- **`tester.pump()` 不带时长参数不推进动画时间** —— 会读到静止值，曾据此做两轮错误修复。
  正确：`await tester.pump(const Duration(milliseconds: 100));`
- **`previewTheme` 必须提供 `MaterialLocalizations` + `Overlay`**，否则含 `TextField` 的组件崩溃；
  ⚠️ **locale 必须传 `en`** —— `DefaultMaterialLocalizations.delegate` 只认英文，传 `zh_CN` 会**静默返回 null**。
- **`MaterialApp` 的 `home:` 必须包 `Scaffold`**，否则文字出现黄色双下划线。
- **不能在 `initState` 读 `MediaQuery`** → 放 `didChangeDependencies`。
- **条件表达式 `? 1 : 0` 是 int**，Dart 不提升为 double，传给 `Opacity.opacity` 会崩且 **analyzer 不报**。
- **`StackFit.expand` / `Center` 在无界高度下的陷阱**：`Stack(children:[Positioned.fill])` 在
  `Column(mainAxisSize.min)` 里会 `BoxConstraints forces an infinite height`；`Center` 在无高度约束下
  **会撑满父级**（顶栏模块项因此从 44 被撑到 64）。需要固定尺寸时用显式 `SizedBox(height:)`。
- **`BackdropFilter` 必须配裁剪**（`ClipRRect`/`ClipRect`）：模糊**不受布局尺寸限制**，
  缺裁剪会把背后**整块画布**糊掉（用户实测「底栏加了个遮罩挡住整个大画布」）。
- **`TabBadge` 内部自带 `Positioned`**：外层再套一层会触发 `Competing ParentDataWidgets` 断言。
- **`AvatarHalo` 在线时有呼吸动画** → 渲染尺寸会抖动；断言应量 **widget 属性**（`halo.size`）而非 `getRect`。
- **`AylaIconData` 不是常量表**（图标在 `kAylaIcons`）→ 用 `aylaIconByName('iconShare')!`。
- **换 `MaterialApp.home` 不会重建子树**（同类型 widget）→ 测试里改参数要用 `StatefulBuilder` 宿主。
- **跨 `pumpWidget` 换 widget 类型后再 `find`** 会同时命中新旧树 → 拆成两个独立 `testWidgets`。

---

## 六、验收标准（每次交付都要走）

```bash
# ① 静态分析零告警
cmd.exe /c "cd /d E:\Elysium-AyerElysia\Elysium\Ayla\flutter && E:\flutter-3.47.4\bin\dart.bat analyze lib"
# ② 全量测试串行通过（**当前基线 208/208**）
cmd.exe /c "cd /d ...\flutter && E:\flutter-3.47.4\bin\flutter.bat test --reporter failures-only --timeout 60s --concurrency 1"
# ③ 每个新组件：@Preview + 加入 component_gallery.dart
# ④ 提交：子仓库（精确暂存本任务文件）→ 父仓库（同步指针 + 子提交全文）
# ⑤ 涉及启动/页面接线时：由用户手动启动验收，agent 不代启
```

**提交时要排除**（M0 阶段遗留，不属于任何批次）：
`flutter/lib/core/{api,net,ws}/`、`flutter/lib/core/app_init.dart`、`flutter/lib/state/`、
`flutter/lib/tool/`、`flutter/web/`、`flutter/devtools_options.yaml`、`docs/flutter/08-*.md`，
以及他人改动的 `docs/flutter/06-开发步骤.md`。
（`flutter/lib/core/media/*.dart` 是正式交付物，需**单独精确 add**。）

**文档同步**：改动落地后同步更新 `13-工作进度与待办.md` 与当日 `.workbuddy/memory/` 日志；
文档与代码属**同一次交付**，不留到「以后补」。

---

## 七、当前 git 状态（2026-09-21）

```
子仓库 Ayla:  0107577 fix(flutter): 顶栏选项卡补齐按压/扫光/入场动画（按范本复用，非自造）
              f1f2c13 fix(flutter): 全局图标色改回 indigo（分享图标变黑）+ 浮层统一文本作用域 + 图标垂直校准
              19ae3dd docs(flutter): Shell 件④响应式顶栏交付记录 + 剩余项更新（子 a262c65）
              a262c65 feat(flutter): Shell 件④——响应式顶栏 AylaTopNav（宽屏圆角卡 / 窄屏方角条）
              0bd789b feat(flutter): Shell 件②③——窄屏底栏重做 + 群内顶栏（按 web 完整调用链）
父仓库:       884eab24 chore(Ayla): 同步子模块指针 → 顶栏交互补齐（0107577)
              （子模块显示 -dirty 是未跟踪的 M0 遗留文件所致，指针本身是一致的）
```

---

## 八、与用户协作的节奏（本会话总结，务必遵守）

（开工动作见 §1.6，此处不重复。）

- 用户会**逐条**指出不符项（顺序、圆角、尺寸、hover、扫光、对齐…）。**不要辩解，先核实再改**；
- **改完立刻 `analyze`**，不要攒着；
- **不要问「要不要继续」**：用户说过「别老是停下来汇报」「我说够就是够，多看，仔细一些」；
- 但**危险/越界操作必须问**（commit、构建、改既有约定、动 M0 遗留文件）；
- 汇报用**简洁中文执行摘要**：改了什么、依据哪一行、验证结果（测试数）；
- 用户给的**实测校准值优先于 web 原值**（例：模块图标垂直偏移，web 是 `-2px`，用户实机校准为 `+1`）——
  此时**必须把偏离 web 的事实与理由写进代码注释**，避免后面的 agent 又「复刻」回去。

---

## 附：本文件与其它文档的分工

| 文件 | 放什么 |
|---|---|
| **本文（11 号）** | **每次都要遵守的**：任务书（§一）、约束、环境命令、语义差、坑、验收、协作节奏 |
| `13-工作进度与待办.md` | **做到哪、下一件做什么**：进度表、各件事实源与要点、决策存档、已知未修问题 |
| `14-组件库复用索引.md` | **范本索引**：按「我要做…」查现成件（**动手前必查**） |
| `06-开发步骤.md` / `05-实施提示.md` | 历史操作手册与配方速查（早期批次写的，作参考） |
| `10-交接-Batch4与当前状态.md` | B4 时期交接（含组件清单 §2，仍可参考） |
