# Ayla Flutter 组件库 —— 复用索引与范本库

> **这份解决一个反复发生的失败**：遇到交互就跑去读 web CSS 自己实现，而库里早有范本。
> 用户 2026-09-21 原话：**「不止是看web。这是已经有组件做好了范本的，学」**。
> **新写任何交互前先查本文件**；查不到再读 CSS。

---

## 一、交互范本索引（按需求查）

| 我要做… | 范本 | 位置 | 关键点 |
|---|---|---|---|
| **选项卡 / 分段控件** | `AylaSegmentedTabs` + `AylaSegmentedTab` | `widgets/primitives.dart:322–925` | 容器级单实例胶囊、`AnimatedPositioned` 迁移、`variant` 档位机制 |
| **选项卡（带筛选/home 语义）** | `AylaDirectoryFilters` + `_FilterTab` | `widgets/profile_and_filters.dart:284–903` | **交互范本之王**（见 §二） |
| **共享高亮胶囊（跨项迁移）** | `AylaNavHighlight` | `widgets/primitives.dart:525–767` | `layoutId` 语义、`setSweep(bool, {jump})`、`showBorder` 默认 true |
| **按压缩放（`:active .98`）** | `AylaPressScale` | `theme/buttons.dart:48–185` | `onPressChanged` 回调、`hoverScale`/`pressScale` 开关 |
| **按钮扫光（`::after`）** | `GlassButton` 的 `_sweep` | `theme/glass.dart:756–765 / 884–908 / 1075–1085` | 600ms、`-120%→120%`、hover `forward()` / 移出 `reverse()` |
| **图标钮（玻璃小卡）** | `AylaIconButton` | `theme/buttons.dart:189–330` | 40×40、`square` → `radius-input`、`sweep` 可选 |
| **菜单项 hover（含防闪灰）** | `_MenuItem` | `widgets/conversation_more_menu.dart:675–743` | hover 冰蓝 + **同色相零透明** |
| **玻璃条 / 卡面** | `GlassSurface` / `GlassCard` | `theme/glass.dart:74–230 / 484–560` | `radiusOverride` / `borderOverride` / `blur`（24 卡 / 18 导航 / 8 按钮） |
| **外阴影（只画形状之外）** | `AylaGlassShadow.ring` / `.animatedRing` / `.fadeRing` | `theme/glass.dart:265–350` | **绝不用裸 `boxShadow`**（会铺进内部） |
| **内高光（`--glass-inset`）** | `AylaInset.topHighlight` / `AylaGlassInset.over` | `theme/glass.dart:390–470` | 非均匀 border 时用 `AylaInset` |
| **入场动画** | `AylaRevealItem` / `AylaRevealScope` | `widgets/reveal.dart` | `offset: Offset(0,-20)` = `panel-from-top`、stagger 50ms cap 300 |
| **浮层（锚定 + 点外部关闭）** | `aylaOverlayEntry` / `aylaOverlayScope` | `widgets/overlays.dart` | **所有 `OverlayEntry` 必须走它** |
| **模态卡片** | `AylaModalCard` | `widgets/dialogs.dart` | 宽屏入场 `wideEntrance`、窄屏 `narrowHeightFactor` |
| **输入框** | `GlassInput` | `theme/glass.dart:1131–1260` | 材质族、`focus` 辉光边、`minLines/maxLines` |
| **头像（带光环/呼吸）** | `AvatarHalo` | `widgets/avatar_halo.dart` | `size` 是**头像直径**、在线时有呼吸动画（断言用 widget 属性） |
| **徽标** | `TabBadge` | `widgets/tab_badge.dart` | metrics 档位、**内部自带 `Positioned`**（外层别再套） |
| **渐变工具（CSS 等价）** | `cssLinearGradient` | `theme/css_gradient.dart:42` | 需传 `aspectRatio` 才等价 CSS 的渐变长度 |
| **图片（含示例图开关）** | `ResourceImage` | `widgets/resource_image.dart` | `previewImage` / `ignoreSampleMedia` |

---

## 二、`AylaDirectoryFilters` 为什么是「范本之王」

它把 CSS 的动态语义完整翻译成了 Flutter 写法，其余组件遇到同类问题**照抄即可**：

| 文件位置 | 解决的问题 | 抄什么 |
|---|---|---|
| `:344–354` | `:hover` 是**每帧求值**的谓词，而 `MouseRegion` 只在指针移动时触发 | 记**索引** + 在 `build` 里求值 `_hoveredIndex == 选中索引` |
| `:356–366` | 胶囊在容器级 → 不继承按钮的 `:active` 缩放 | `_pressedIndex` + `GlobalKey<AylaNavHighlightState>` |
| `:405–424` | 高亮尺寸 = 按钮实测矩形，不能按等分推算 | `GlobalKey` + `addPostFrameCallback` + `localToGlobal(ancestor:)` |
| `:555–587` | 共享胶囊迁移 | `AnimatedPositioned`（300ms `[0,0,.58,1]`）外包 `AnimatedScale`（同步 `.98`） |
| `:527–550` | 「挂载即命中」：点击时 `:hover` 首帧已匹配，**不产生 transition** | `setSweep(true, jump: true)` |
| `:737–802` | 零透明必须**同色相**（透明黑插值会闪灰） | `color.withValues(alpha: 0)` 而非 `Colors.transparent` |
| `:805–829` | hover 阴影**不能裸用 `boxShadow`**（会染进内部） | `AylaGlassShadow.ring` + `AnimatedOpacity` |
| `:880–898` | 按压态上报 | `Listener.onPointerDown/Up/Cancel` → `onPressedChanged` |

---

## 三、组件库纪律

1. **复用优先**（用户三次点名）：先看整个组件库；没有实现才新增。
2. **看起来像但不完全一样** → **先想加档位，再想新建**；档位保留并在注释写明「当前用哪档 + 另一档为何存在」。
3. **不要凭印象关闭库内默认样式**：改任何参数前先读该参数的**默认值**与 **web 规定**。
   事故：我曾在底栏/群顶栏/顶栏三处凭空传 `showBorder: false`，把 1px 白边全关了。
4. **不要写死库内已表达的属性**：如 `TextStyle.height`（会破坏行高继承）。
5. 新增基础件前，先 `grep` 这些关键词确认无现成实现：
   `sweep` / `setSweep` / `AnimatedScale` / `onPressChanged` / `AylaNavHighlight` / `MouseRegion` /
   `AylaGlassShadow` / `AylaGlassInset` / `cssLinearGradient` / `AylaRevealItem` / `aylaOverlayEntry`。

---

## 四、易错点速查（写代码前扫一眼）

- `AylaIconData` **不是常量表** → 用 `aylaIconByName('iconX')!`；
- `AylaIcon` 颜色解析：显式 `color` → 祖先 `IconTheme` → `textPrimary`；**主题必须显式设 `iconTheme`**
  （否则 SDK 注入 `Color(0xDD000000)` 纯黑）；
- `StackFit.expand` 在无界高度下崩（`BoxConstraints forces an infinite height`）；
- `Center` 在**无高度约束**下会撑满父级（顶栏模块项因此 44→64）；
- `BackdropFilter` 必须配 `ClipRRect`/`ClipRect`（否则糊掉整块画布）；
- `OverlayEntry` 是独立子树 → 文本必须走 `aylaOverlayEntry` 兜底；
- 换 `MaterialApp.home` 不重建子树（测试改参数用 `StatefulBuilder`）。
