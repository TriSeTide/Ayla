# Ayla Flutter 复刻 —— 工作进度与待办

> **这份是「做到哪、下一步做什么」的唯一事实源。** 与 `11-新对话启动提示词.md`（约束与坑）配套使用。
> 最后更新 2026-09-22（**B2 live 域第三批「直播侧栏 + 开播选择器」已交付（待用户验收/提交）**：
> `AylaLiveChannelRail` / `AylaLiveStartSheet`（§六 6.30）；同批把 `channel_sidebar` 的私有虚线 painter
> 提为共享件 `AylaDashedBorder`；**B2-4「观看条/名单弹层 + 主播头像 + 推流地址」**（§6.31）与
> **B2-5「建播表单 + 控制台资料栏」**（§6.32）已交付；同轮把**禁用态改为按颜色降透明**（§6.33，
> 用户批准的全库统一项）；基线 **631 通过 / 0 失败**）。
> 本文件基于子仓库 **`7cb745b`** / 父仓库 **`1b739537`**（B2 三批均**未提交**——用户未下令 commit）。
> ⚠️ **以 `git log -1` 为准**（文档不可能引用「包含自己的那次提交」之后的 hash）。
> ⚠️ **工作树当前**：B2-1/2/3 三批 + 三域统一 + 库内扩展 **全部未提交**
> + **用户本人的改动**（`primitives.dart` 的 `AylaScrollingTags` 遮罩/接缝修复 + 新增
> `test/scrolling_tags_test.dart` —— **勿动、勿回退**）+ 他人改动 `docs/flutter/06-开发步骤.md`、
> `flutter/lib/main.dart` + M0 遗留**未跟踪**文件（见 §五，**勿提交**）。
> 最近一次 Windows release 构建**通过**（63.7s EXIT=0，产物含 voice 域 8/8；**本轮改动未构建**）。

---

## 一、总进度一览

| 阶段 | 状态 | 说明 |
|---|---|---|
| M0 基座（网络/状态/路由/WS） | ✅ | 文件已就位（**未跟踪**，见 §五注意事项） |
| Batch 1–4：组件库基元 | ✅ | 玻璃族 / 按钮族 / 卡片族 / 目录筛选族 / 输入族 |
| Batch 5：业务组件族 | ✅ 大部分 | 帖子卡 / 编辑器 / 评论 / 查看器 / 分享 / 媒体能力层 |
| **Shell 层** | 🔄 **A 档已做完** | 底栏 ✅ / 群顶栏 ✅ / 响应式顶栏 ✅ / **服务器列 + 频道侧栏 ✅** / **CreateSheet ✅（A3）** / **右下浮层按钮族 ✅（A4）** / **会话活动悬浮球 ✅（A5）**；剩 `AppShell`（A6，等页面层） |
| **B 档组件域** | 🔄 **进行中** | **B1 voice 域 8/8 收官** ✅；**B2 live 域第三批「直播侧栏 + 开播选择器」✅**（`AylaLiveChannelRail` / `AylaLiveStartSheet`，**7/16**，§六 6.30）→ 下一件 = `LiveCreate`（域内顺序见 §二 B） |
| 页面层 | ⏸ 未开始 | **用户明确要求「后面统一做」**；此前提前做的帖子流页面已被要求 revert |

当前测试基线：`flutter test --concurrency 1` → **631 通过 / 0 失败（全绿）** —— 既存 459 + 弹幕族 55 +
语音卡 head 回归 1 + 直播大厅 35 + 分享键尺寸 2 + 等高/可见性审计 5 + 直播侧栏 23
+ 弹窗组合与限高 4 + 观看条/名单 16 + 头像/推流 10 + 建播/资料栏 14 + 禁用态回归 4
+ **用户新增的 `scrolling_tags_test` 6**；`dart analyze lib` 零告警。

> **重做提示词**：`15-重做频道侧栏-启动提示词.md`（ChannelSidebar 重做专用，含 9 条已踩坑）；
> 推导表：`16-频道侧栏推导表.md`（逐条 web 行号 → 实现）。
Windows release 构建通过（增量 21.8s / EXIT=0）。

---

## 二、Shell 层进度（当前主战场 —— 你要做的就是这里）

> ⚠️ **边界**：本节全是**壳层组件**（`lib/widgets/*.dart`），**不是页面**。
> 页面层（`lib/pages/**`、路由、页面级编排）用户明确要求「后面统一做」，**现在一件都不许写**。

### ✅ 已交付并验收

| 组件 | 文件 | 提交 | 要点 |
|---|---|---|---|
| `AylaBottomTabs` 窄屏底栏 | `widgets/bottom_tabs.dart` | `0bd789b` | **方角**（顶层 `20 20 0 0` 被 `@media(max-width:768px)` 447–448 清零）；`GlassSurface` blur18 + 顶部 1px 边 + 安全区同面；按钮 `margin 4px 2px`；容器级共享胶囊跨槽 300ms 迁移 + hover 扫光；`gap 2px`；主页圆盘 `margin-top:-8`（上浮 8、占位 40）；**F1 不渲染红点** |
| `AylaGroupTopTabs` 群内顶栏 | `widgets/group_top_tabs.dart` | `0bd789b` | 中央换群头像 48 + `margin-top:-6`；图标 22；`border-bottom`；`--glass-shadow-compact`；按钮 `inline-flex` 按内容宽（胶囊须 **GlobalKey 实测按钮矩形**）；帖子 tab 8px 粉点；场景枚举含 **`chat`**（点头像=两级语义，切 chat 时**清空胶囊**）；动画按 web 分层（组件只接受父级 `offset`） |
| `AylaTopNav` 响应式顶栏 | `widgets/top_nav.dart` | `a262c65` + `0107577` | **一个组件两形态**（≤768 窄屏 / >768 宽屏）；宽屏 = **复用 `GlassCard`**（浮动圆角卡）+ 高 64 + `Stack(alignment:center)`；模块链顺序取 `shellConfig.ts:22–28` 的 `PRIMARY_MODULES`（**主页/语音/直播/帖子/桌游**）；模块项**固定高 44**；高光/迁移/按压/扫光**复用 `AylaDirectoryFilters` 那套**；胶囊白边走库内默认；消息钮 = 底色 .18 **与**底条两者都要；搜索框走文本字段族；浮层**相对 Overlay 求坐标 + 锚点右下角**；窄屏 = 方角条高 56 + 三 variant + 入场 `AylaRevealItem(-20px)` |

| `AylaServerRail` 宽屏服务器列 | `widgets/server_rail.dart` | `e6d659b`（2026-09-21 已交付） | 72px 玻璃列；外距 12/0/12/12（auroraqua 270 覆写右 0）；**内 1px 占位**——CSS `border` 占布局、Flutter `BoxDecoration(border:)` 不占，缺这 1px 时行宽 72≠70、浮层锚点整体偏 1px；列表 padding 20/77 + 行距 12 + `clip-path` 15 + 上 20/下 16 `mask-image` 渐隐（ShaderMask `dstIn`，stops 按实高换算）；头像 48 + 光环，选中 `scale(52/48)` 180ms；指示条 **3×32 `--glow-500`**（`group.css` 549–559 的 `::before` 版被 auroraqua 216 `content:none` 关掉 → 实际是 `--rail` 变体，为此给 `AylaNavHighlight` 加 `rail` 档）；未读 = 消息未读 + 帖子未读（左下角 -3/-3、99+、`TabBadge` 新增 `serverItem` 档）；置顶 pin 左上 -6/-4 45° 粉；状态角标复用 `AylaAvatarStatusBadges`；悬停 180ms 展开置顶面板（**浮层走 Overlay**，见 §六 6.4）；底部 53 加号复用 `AylaIconButton(size: 53)`；入场 `panelVariants(left)` 左入 20 / 300ms easeInOut；可点区域 = 头像本身（web `button` 只包头像）；**滚动条已关**（web `base.css` 372–383 全局隐藏原生滚动条 → Flutter 桌面端自动 `Scrollbar` 会占宽并引起布局跳动，用 `ScrollConfiguration(scrollbars: false)` 关闭；自绘覆盖层条属 §B6 `OverlayScrollbar` 待做） |

| `AylaChannelSidebar` 宽屏频道侧栏（**A2 重做**） | `widgets/channel_sidebar.dart` | `89458a5`（2026-09-21 已交付） | slot 284（260+2×12）+ `GlassSurface` radius16/blur24/**内 1px 占位**（CSS border 占布局，缺它则列表轨道 242→244、浮层钮偏 1px）；群名头 Fredoka 500 20 + 16 chevron（**私有 glyph**，tsx 605–627 的内联 SVG 不在 icons.tsx）；场景项 40/gap12/padding0-16/radius12；**状态标识三型**——语音在麦人数与 LIVE 是**裸文本**（web `.channel-scene-status` 零样式，继承 15px/600/secondary）、帖子未读才是粉徽标（`margin-left:auto` 贴右，新增 `TabBadge` 的 `channelBadge` 档 = Fredoka 11/**w600**/line-height 16/11）；三个下拉各挂一个 **paint-only 裁剪层**（等价 `useSidebarContentClip`；**命中也随裁剪**，与 CSS `clip-path` 一致）；**自建 sticky**——chat 0 / voice 44+吸底52 / live 88+吸底8，行本体画在浮层、在 **paint** 阶段按同帧几何定位（`performLayout` 自写 + `applyPaintTransform` 同偏移 + 首帧后补一次重绘）；**两套 hover**（按钮本体→扫光 700ms，行级含浮层钮→只驱动底色 .18）；**按压三档**（场景项与三行只有 `:active .98`；三个三角键属按钮组 hover 1.02+active .98；＋/笔不在任何组）；胶囊 **4 个实例**（scene/subgroup/live 跨项迁移 + 语音房行内独立，`sharedLayout={false}`），容器级单实例用 paint 定位 + 300ms 迁移、位置变化直接跟随；语音房列表 **稳定父级 + 按索引 `AnimatedPositioned`**（活跃度重排 300ms）；切群 **旧面板先退场再挂新面板**（`AnimatePresence mode="wait"`，退场期间冻结旧群 props）；**弹窗接线未做**（＋/笔点击暂无副作用）；样张 `aylaChannelSidebarSamples()` **可交互**（内嵌状态宿主，一级与二级选项卡都能点）|

| `AylaCreateSheet` 通用创建浮层（**A3**） | `widgets/create_sheet.dart` | `6def56c`（2026-09-21 已提交） | 容器三件套分工——`AylaModalOverlay`（遮罩 + 宽屏居中 / 窄屏贴底）+ `AylaModalCard`（`--glass-bg-strong` / blur24 sat1.4 / 1px 亮边 / radius-panel 20 / modal 阴影 / 窄屏 250ms 上滑）+ 新公共件 `AylaSheetHead`；⚠️ **overlay 与卡片 padding 都是 `sp4 = 16`**（`AylaModalOverlay.padding` 默认是 24，那是 privacy 档，必须显式传）；head = Fredoka 18/600/textPrimary + `.icon-btn-40` 关闭钮（`IconClose` **20** —— 与 ConfirmDialog 那处的 18 逐处不同）；三条关闭路径（ESC = `Shortcuts + Actions(DismissIntent) + FocusScope(autofocus)`，范本 `share.dart` ／ 点遮罩 ／ 关闭钮；**点卡内不关**）；`role=dialog aria-label` → `Semantics(scopesRoute + label)`；窄屏 width 100% / radius 24 24 0 0 / 去左右下边框 / `padding-bottom calc(sp4 + safe-area)`；tsx 的 `className` 等价映射为 `maxHeight` / `narrowHeightFactor` / `scrollable` / `narrowRadius` 透传；**顺带修正** ConfirmDialog 与 PrivacySheet 的关闭钮（原先手搓 40 盒 + Material `Icons.close` + 次要色）→ `AylaSheetHead` / `AylaIconButton` + `iconClose 18`；样张**可交互**（关闭 → 「重新打开」还原；两形态用 `MediaQuery.copyWith(size:)` 覆写局部视口） |

| `AylaCornerFabStack` / `AylaRefreshFab` / `AylaScrollTopFab` / `AylaQuickMessageFab` 右下浮层按钮族（**A4**） | `widgets/fab.dart` | `6def56c`（2026-09-21 已提交） | 四件一批：堆叠容器 = `fixed right 38`（`32 + (56-44)/2`）/ `bottom 100`（`32 + 56 + sp3`）/ column · `gap 12` · `align-items: flex-end` · 容器不吃指针（Flutter 裸 `Column` 天然等价，**别**加 `IgnorePointer`）；44px 玻璃钮**复用 `AylaCornerFab`**（`--glass-bg` + blur18 sat1.4 + `--card-shadow`，hover → strong + `0 2px 12px .18`）；**顺带修正该件过渡 180 → 200ms**（`auroraqua.css:54–94` 把 `.corner-fab` 并入按钮组、同特异性后加载覆盖 `shell.css:713–714`）；刷新钮 = `iconRetry 20`，spinning = `ayla-loading-spin 800ms linear infinite`（reduced-motion 不转），**无回调时按钮照常可点只是无动作**（不是 disabled）；回顶钮 = `iconArrowUp 20`，`pixels > 视口高` 且命中**主滚动容器**（`viewportDimension ≥ 40% 视口高`）才浮入（`opacity + translateY 8→0`，200ms；隐藏态 `IgnorePointer + ExcludeSemantics`），点击 smooth 回顶（300ms ease-out；reduced → `jumpTo`）；消息钮**复用 `AylaMessageFab`**（`iconMessage 24` + `TabBadge`），4s 无点击 → 半贴 `translateX(-44px)`（200ms `--ease-out`），半贴点击点出来、展开点击打开快捷栏；定位档 `bottomLeft`（`left 32 / bottom 32`）与 `narrow`（`right 16`→stacked `22` / `bottom 76+safe`→stacked `144+safe`）由组件返回 `Positioned`；样张 `aylaFabSamples()` **可交互**（滚列表看回顶浮入 / 点刷新看旋转 / 等 4s 看半贴） |

**其它已修**：`BackdropFilter` 漏裁剪（`7723ac2`）、全局图标色被 SDK 黑色 `iconTheme` 污染（`f1f2c13`，新增 `widgets/overlays.dart` 统一浮层文本作用域）。

| `AylaSessionActivityIndicator` 跨页面媒体会话悬浮球（**A5**） | `widgets/session_activity.dart` | 2026-09-21（**待用户验收/提交**） | 固定层（web `position: fixed`；right 24 / top 80 / z 55；窄屏 right 16 / top `calc(56 + safe-top + 48)`）= 语音球 + 直播球 + 收起把手 · 球 44×44：1px `--glass-border` + **不透明** `sakura-100`/`ice-300` 底（字色 `grape-700`/`indigo-700`）+ `0 2px 12px rgba(70,91,146,.12)`（**不是** `--card-shadow` 的 .08，也不是 `.18`），hover/focus → `scale(1.08)` + `--glow-shadow`（150ms `--ease-out`；**不在** auroraqua 按钮组 ⇒ 无 1.02/.98；**不加**背板模糊——不透明底把 blur 完全盖住）· 把手 28×44 玻璃（`--glass-bg-strong` + blur18 sat1.4 **保留**）+ `›` 字符 16/w500/line-height 1；200ms `--auroraqua-ease` + hover 1.02 / active .98（复用 `AylaPressScale`）；hover/**focus-visible** 底色 **透明**（`--glass-bg-hover` 全历史未定义 ⇒ 实渲染回落初始值）+ 字色转 `--text-primary`；图标 rotate 180° 200ms `--ease-out` · 收起：整组右移 24（窄屏 16 ⇒ 把手贴屏幕右缘）+ 球 `translateX(64px)` 淡隐 + 收起态球不可点 / 不进语义 / 不进 tab（**收起 transform 200ms、展开 transform 150ms、opacity 两向 200ms**——CSS 两条规则的 transition 时长不同，见 §六 6.21） · 拖动：**5px** 阈值、`clamp(8, 视口高-52)`、拖动后抑制合成 click、鼠标仅主键；**几何走原始指针事件**（识别器要等 18px kTouchSlop）+ 空 vertical drag recognizer 赢竞技场（= web `touch-action: none`，测试锁 `controller.offset == 0`）· 返回 `Positioned`（同 A4 约定）· 样张 **可交互**（收起/展开、拖动、点球回调、会话开关）· 依据与三条拍板见 §六 6.21 |

### ⏳ 待做（**完整缺口清单**；一次一件，按顺序推进）

**缺口总览**（2026-09-21 对照 web 盘点）：web 共 **11 个组件域 + 21 个顶层散件 + 14 个 layout 件**；
Flutter 侧已交付处见上表（A5 落地后 **Shell 层 A 档只剩 A6 `AppShell`**，它与页面层同期做）。
**下列是尚未开工的全部部分。**

#### A. Shell 层剩余（先做完这一档）

| # | 组件 | web 事实源 | 规模 |
|---|---|---|---|
| ~~A1~~ | ~~`AylaServerRail`~~ → **2026-09-21 已交付**（见上表 + §六 6.4） | — | — |
| ~~A2a~~ | ~~`AylaChannelSidebar` 主体~~ → **2026-09-21 已交付**（见上表 + §六 6.8/6.9） | — | — |
| ~~A2~~ | ~~`AylaChannelSidebar` **重做**~~ → **2026-09-21 已交付**（见上表 + §六；弹窗接线仍未做） | — | — |

**A2 交付后的补修（2026-09-21，用户逐条验收指出，已提交 `89458a5` + 后续一轮）**：

1. **三组独立共享胶囊**：web 的 `selectionId-scene` / `-subgroup` / `-live` 是**三个各自独立的
   layoutId、可同时存在**；此前做成「全组件单实例」⇒ 父级（场景组）永远没有胶囊、子项收起时
   旧矩形被误用（用户：「父级选项卡没有，二级选项卡还窜到父级去了」）。
2. **胶囊 z 序**移到内容**之下**（web `z-index:-1`）；画在内容之上会盖住图标与文字。
3. 子群 / 直播间胶囊各套一层与**各自下拉同参数**的裁剪层 —— web 里胶囊是按钮子元素，被
   `.channel-subgroups` 的 `overflow:hidden` 裁住；卡片级单画会**超出视口**（用户实报）。
4. **扫光「挂载即命中」统一**：抽出 `_jumpSweep(kind)`（**排到下一帧**——点击那刻胶囊可能刚挂载、
   `currentState` 还是 null）；底部两项（帖子/桌游）此前漏了该调用 ⇒ 点击后**从左往右重播一次**
   才反向（用户实报）。5 处（场景/子群/语音房/直播间/底部两项）行为现已一致。
5. **帖子/桌游不再渲染浮层钮**（tsx 511–522）：`hasSecondary` 由 `_ => open` 改为 `=> false`，
   此前 ＋/三角盖住按钮右端 ⇒ 该区域 hover 只点亮行级 ⇒ **扫光不触发**。
6. **语音房「高亮移动动画」照 web**：`sharedLayout={false}` ⇒ 不做跨行共享迁移；高亮是
   `motion.li layout="position"` 的**子元素** ⇒ **随行一起位移**（Flutter 侧由行的
   `AnimatedPositioned(top: i × pitch)` 驱动）。同时**去掉一切自造入场淡入**（web 用
   `initial={false}`）—— 见 §6.13。
7. **点「直播」默认选中第一个直播间**；**「语音」保持不默认选中**（用户裁决：语音房有独立页面，
   必须手动点）。「聊天」保持默认选中第一个子群（tsx 451）。
8. 样张：语音房列表自持**活跃排序**演示（点某房 → 视为「有人进入」→ 排到最前，行平滑位移）。
9. **三角展开键常态不亮**：`.is-open` **只旋转**（web `group.css 874–876` 只有 `transform`），
   底色/字色**只有 `:hover` 才给**（883–886）。此前把 `is-open` 当 `activeState` 传 ⇒ 只要下拉
   展开三角键就常驻亮底（用户实报「常态时这里不高亮的」）⇒ 拆出 `rotated` 参数专管旋转。
10. **同一行的多个浮层钮分开高亮**：web 每个钮有自己的 `:hover`（`-voice-add:hover` 852 /
    `-voice-toggle:hover` 883），但**两者都联动词条底色**（878–881）⇒ 拆成
    `_hoveredRow`（行底联动）+ `_hoveredOverlay`（钮自身，身份 = `overlay:kind:id:slot`，
    `slot ∈ edit/add/toggle`）。此前两钮共用行级身份 ⇒ 悬停一个两个一起亮（用户实报）。
| ~~A3~~ | ~~`AylaCreateSheet`~~ → **2026-09-21 已交付**（见上表 + §六 6.18；**已提交 `6def56c`**） | — | — |
| ~~A4~~ | ~~`AylaCornerFabStack` / `RefreshFab` / `ScrollTopFab` / `QuickMessageFab`~~ → **2026-09-21 已交付**（见上表 + §六 6.19；**已提交 `6def56c`**） | — | — |
| ~~A5~~ | ~~`SessionActivityIndicator`~~ → **2026-09-21 已交付**（见上表 + §六 6.21；**待用户验收/提交**） | — | — |
| A6 | `AppShell`（双形态装配） | `layout/AppShell.tsx` | **等页面层一起做**（依赖路由） |

**A1 核实结论**（已落地，供后续 agent 复用）：72px 玻璃列 / 48 头像 + 光环 / **左 3px 指示条** / 角标 16 均属实；两处 auroraqua 覆写已确认——
`:270` 的 `.server-rail { margin-right: 0 }`（外距右 0）与 `:216` 的
`.server-item.has-auroraqua-highlight.is-active::before { content: none }`
（**该行让 group.css 549–559 的 `::before` 指示条永不显示**，真实指示条是子元素 `--rail` 变体）。
复用了 `AvatarHalo` / `AylaAvatarStatusBadges` / `TabBadge`（新增 `serverItem` 档）/
`AylaIconButton` / `AylaDirectoryLoadMore` / `AylaRevealItem` / `AylaNavHighlight`（新增 `rail` 档）；
**`AylaPressScale` 未用**——`.server-item` 不在 auroraqua 的按钮组/导航组 `:is()` 列表内，无 hover 1.02 / active .98。

**A2 已知要点**：240–280px；场景项行高 40 / 圆角 12 / 选中底 `rgba(157,191,230,.35)`；
含群场景、子群、语音/直播切换；属导航组（`auroraqua.css:238–249`：`transition` 300ms + `:active scale .98`）。

#### B. 组件域（Shell 之后逐域推进，**每域一批**）

| # | 域 | 待做文件 | 数量 |
|---|---|---|---|
| B1 | **voice 语音族** | ~~`VoiceChannelCard`~~ ✅ / ~~`VoiceChannelList`~~ ✅ / ~~`VoiceControls`~~ ✅（**B1-1**，§六 6.22）/ ~~`VoiceMemberRow`~~ ✅（**B1-2**，§六 6.23，含覆盖式音量条）/ ~~`VoiceChannelCreate`~~ ✅（**B1-3**，§六 6.24）/ ~~`VoiceChannelPanel`~~ ✅（**B1-4**，§六 6.25）/ ~~`ElysiaVoicePanel`~~ ✅（**B1-5**，§六 6.26）/ ~~`VoiceRoomBody`~~ ✅（**B1-6**，§六 6.27）| **8（8/8 收官）** |
| B2 | **live 直播族** | ~~`DanmakuList`~~ ✅ / ~~`DanmakuInput`~~ ✅ / ~~`DanmakuOverlay`~~ ✅（**B2-1**，§6.28）/ ~~`LiveChannelCard`~~ ✅ / ~~`LiveHall`~~ ✅（**B2-2**，§6.29）/ ~~`LiveChannelRail`~~ ✅ / ~~`LiveStartSheet`~~ ✅（**B2-3**，§6.30）/ ~~`LiveHostAvatar`~~ ✅ / ~~`LiveStreamAddresses`~~ ✅ / ~~`LiveViewerStrip`~~ ✅ / ~~`LiveViewerSheet`~~ ✅（**B2-4**，§6.31）/ ~~`LiveCreate`~~ ✅ / ~~`LiveOwnerPanel`~~ ✅（**B2-5**，§6.32）/ 余 3：`LiveMiniPlayer` → `LivePlayer` → `LiveRoomBody` | **16（13/16）** |
| B3 | **chat 聊天族** | `MessageBubble` / `MessageList` / `MessageInput` / `ConversationList` / `PrivateChatPane` / `MediaContent` / `ShareBubble` / `ElysiaEntry` / `EmojiPackPanel` / `MentionPicker` / `QuickMessagesSheet` / `WideMessagesSidebar` | **12 待做**（`ConversationMoreMenu` ✓ / `ImageViewer` ✓） |
| B4 | **boardgame 桌游族** | `GameRoomCard` / `GameRoomCreate` / `GameRoomPlaceholder` | **3（全缺）** |
| B5 | **group 剩余** | `SubGroupDialog` / `GroupApplyDialog` | **2**（顶栏 ✓） |
| B6 | **顶层散件剩余** | `GroupCreateDialog` / `ProfileContentSections` / `DirectoryResultCards` / `OverlayScrollbar` | **4** |
| B7 | **motion 待做** | `PageTransition` / `PrimaryNavPage` / `ConversationTransition` / `FullScreenSwipeBack` | **4** → **等页面层**（已并入：`AuroraquaNavHighlight` → `AylaNavHighlight` ✓、`PullToRefresh` ✓） |
| B8 | **layout / 路由** | `AppShell` + `NavigateBridge` / `ProtectedRoute` | **3** → **等页面层** |

> **域内推进建议**：先做**独立性强、样式自洽**的（弹幕、卡片、行）→ 再做**容器/面板**
> （`RoomBody` / `Panel`）→ 最后做**依赖数据流的**（`List` / `Input`，通常与页面接线耦合）。

#### C. 全库统一项（贯穿始终，做完一批顺手补）

> ⚠️ **不要顺手做的两项**（用户 2026-09-22 明确）：
> ① **原生滚动条隐藏**：web 用 `base.css:372–383` 全局隐藏，但 Flutter 侧**只保留
> `server_rail` / `channel_sidebar` 两处历史改动**，其余组件**不要**批量加
> `ScrollConfiguration(scrollbars: false)`；自绘覆盖层条属 §B6 `OverlayScrollbar`（待做）；
> ② 组件库的**跨组件一致性改动**（同一模式在多个组件里改）**一律先问用户**再动手。

- ~~**禁用态「按颜色降透明」**~~ ✅ **已于 2026-09-22 完成**（§6.33：`GlassButton` + `buttons.dart`
  的 `AylaIconButton`/`AylaCornerFab`/`AylaToolButton`；原因 = 整层 `Opacity` 叠 `BackdropFilter`
  被 Impeller 拒绝且禁用态不变暗）；
- **`.msg-action-btn` 缺 `blur(8px)`**（web auroraqua 124–132 有、本库该件未实现模糊层）——登记待补；
- **`Tooltip` 悬停提示**（见 §4.2）：web 图标钮普遍带 `title={label}`，Flutter 全库零 `Tooltip`；
  **建议在 B 档推进时一次补齐**，别只给单个组件加；
- **`MediaSigner` 同源替换修复**（见 §4.1）：影响所有签名媒体，修法已明确；
- **媒体/视频接线**（见 §4.3）：能力层已齐，接线属页面层。

#### D. 页面层（**最后统一做，现在一件都不许写**）

web `pages/` 下全部页面 + `AppShell` 装配 + 路由 + `NavigateBridge` / `ProtectedRoute` + motion 转场族。
**用户明确要求「页面是后面统一做的」** —— 组件库补全之前不要碰。

---

## 三、组件族本会话完成的扩充（供复用参考）

- `widgets/overlays.dart`（**新**）：`aylaOverlayEntry` / `aylaOverlayScope` —— 浮层统一入口，
  **所有 `OverlayEntry` 必须走它**（否则 `Text` 落到 `DefaultTextStyle.fallback` = 双下划线黄线）。
- `theme/app_theme.dart`：`buildAylaTheme()` 显式设 `iconTheme: textPrimary`（**不能靠 SDK 缺省**，那是纯黑）。
- `theme/buttons.dart`：`AylaIconButton` 新增 `sweep` 参数（600ms 扫光，照 `GlassButton` 写法）；
  `AylaPressScale` 的 `onPressChanged` 用于容器级胶囊同步 `.98`。
- `widgets/reveal.dart`：`AylaRevealItem` / `AylaRevealMotion` —— 入场动画（`offset: Offset(0,-20)` = `panel-from-top`）。
- `widgets/profile_and_filters.dart`：**交互范本库**——`AylaDirectoryFilters` 的选项卡实现（共享胶囊迁移、
  按压同步、扫光直达、`setSweep(jump:)`）是同类需求的首选参照。
- `widgets/primitives.dart`（2026-09-21）：`AylaNavHighlight` 新增 `variant` 档位——
  `nav`（默认，既有 6 处调用不变）/ **`rail`**（ServerRail 的 3×32 `--glow-500` 竖条：
  纯色底、无亮边、无扫光、radius 2）。
- `widgets/tab_badge.dart`（2026-09-21）：新增 `TabBadgeMetrics.serverItem`
  （`.server-item-badge`：16×16 / padding 0 4 / Fredoka 11 / **`line-height: 16px`**，
  与 `.group-badge` 的 `line-height: 1` 分档）→ 四档变五档。
- `widgets/menu_item.dart`（**新**，2026-09-21）：`AylaMenuItem` + `AylaMenuItemMetrics`
  （`conversation` = `.conv-menu-item` 40/12/10/14px/icon16；`rail` = `.server-pop-action`
  34/8/8/13px/icon14）。`conversation_more_menu.dart` 的私有 `_MenuItem` 已删除并改用它，
  默认行为不变（全量测试 222/222 验证）。
- `widgets/dialogs.dart`（2026-09-21，A3）：新增**公共件 `AylaSheetHead`**（`.create-sheet-head` +
  `.create-sheet-title` + `.icon-btn-40` 关闭钮 = `AylaIconButton` + `AylaIcon(iconClose)`）——
  web 里 `ConfirmDialog.tsx:82–89` 与 `CreateSheet.tsx:50–55` **共用同一套 head**，
  Flutter 侧此前只有 ConfirmDialog 内联的一份（且是手搓 40 盒 + Material `Icons.close`）；
  `closeIconSize` 设为**必填**（CreateSheet = 20 / ConfirmDialog = 18，逐处按 web）。
  `ConfirmDialog` 已改用它；`PrivacySheet` 的 `.privacy-sheet-head` 是另一套规格（带 16/700 标题 +
  下边框 + `gap sp3`），只把关闭钮换成 `AylaIconButton`。
- `widgets/fab.dart`（**新**，2026-09-21，A4）：`AylaCornerFabStack` / `AylaRefreshFab` /
  `AylaScrollTopFab` / `AylaQuickMessageFab` —— 右下浮层按钮族。**没有新造材质**：
  44px 玻璃钮用 `AylaCornerFab`、56px 消息钮用 `AylaMessageFab`、徽标用 `TabBadge(tab 档)`；
  本文件只补「定位档 / 旋转 / 浮入淡出 / 半贴位移 / 滚动判定」这些 web 侧由 CSS + store 提供的部分。
  `AylaRefreshFab` / `AylaScrollTopFab` 在 `bottomLeft` / `narrow` 档**返回 `Positioned`**
  （web 是 `position: fixed`）→ 调用方须放在页面最外层 `Stack` 的直接子级。
- `theme/glass.dart`（2026-09-21，A5）：`AylaGlassShadow.animatedRing` 新增可选 **`curve`**
  （默认 `AylaCurves.auroraqua`，既有调用点全不变）—— 球的阴影过渡是 `150ms --ease-out`
  （`shell.css:495–496`），与 auroraqua 按钮组的 `ease` 不同；这是「先加档位、不新造」的又一例。
- `core/models/visibility.dart`（**新**，2026-09-21，B1-1）：`AylaPostVisibility` +
  `aylaVisibilityLabels()` 从 `post.dart` 抽出（web 的可见性联合与 `getVisibilityLabels`
  本就是**跨域共用**：posts / voice / live 同源）——`post.dart` 继续 re-export，
  既有 import 调用点零改动（与 `media_kind.dart` 抽出同口径）。
- `theme/glass.dart`（2026-09-21，B1-1）三处档位扩展（都有独立 web 事实源，非自由发挥）：
  ① `GlassButtonVariant.outlineDestructive`（`.voice-leave-btn`：透明底 + destructive 字 +
  1px destructive 边、无阴影、**不叠 `--glass-inset`**）；
  ② `GlassButton.fontSize`（`.voice-join-btn` 13 / `.voice-rejoin-btn` 12，默认仍 14）；
  ③ `AylaCardInteraction.focusRingColor` + `focusRingRadius`（卡片族焦点环是 **`--ice-500`**，
  与 `AylaPressScale` 的 glow-500 不同；环**画在形状之外**、不占布局 —— 库内 `AylaPressScale`
  的内嵌环会长大 4px）；传了环色即表示该卡参与键盘可达性（`Enter`/`Space` → `onTap`）。
- `widgets/voice_room_body.dart`（**新**，2026-09-21，B1-6）：`AylaVoiceRoomBody` +
  `AylaVoiceChatMessage` / `AylaVoiceChatHistory` / `AylaVoiceChatSendException`。
  两张卡与聊天卡内的头/列表/输入条多为复用；私有件：裸 `.btn`、聊天栏开关、未读徽标、窄屏浮层。
- `widgets/elysia_voice_panel.dart`（**新**，2026-09-21，B1-5）：`AylaElysiaVoicePanel`。
  **全复用**：外壳 = `GlassSurface`、按钮 = `GlassButton`（glow / primary / outlineDestructive）、
  输入 = `GlassInput`、「收起」= `AylaMsgActionButton`（`.msg-action-btn` 档）。
- `widgets/voice_channel_panel.dart`（**新**，2026-09-21，B1-4）：`AylaVoiceChannelPanel` +
  `AylaVoicePanelMember` / `AylaVoiceMembersPage` / `AylaVoiceMemberAction`。**几乎全是编排**：
  成员行 = `AylaVoiceMemberRow`、控制条 = `AylaVoiceControls`、分页 = `AylaDirectoryLoadMore`、
  材质 = `GlassSurface`、房主操作 = `GlassButton(ghost)`。
- `widgets/voice_channel_create.dart`（**新**，2026-09-21，B1-3）：`AylaVoiceChannelCreate` +
  `AylaVoiceChannelCreateRequest` + `AylaVoiceChannelCreateException`。**全复用**：
  可见性选择器 = `AylaVisibilitySelector`（含 `lockGroup` / `initialGroupId`）、输入 = `GlassInput`、
  按钮 = `GlassButton(expand)`；容器由页面层用 `AylaCreateSheet`。
- `widgets/voice_member_row.dart`（**新**，2026-09-21，B1-2）：`AylaVoiceMember` /
  `AylaVoiceSelfState` / `AylaVoiceMemberRow`。**没有新造材质**：头像 = `AvatarHalo`（size 32 /
  `AvatarCore.elysia` 光环 / `online` / `onTap`）、图标 = `AylaIcon(iconMic/iconMicOff/
  iconSpeaker/iconSpeakerOff)`、开关壳 = `AylaPressScale`（它在 auroraqua 按钮组内）。
  **覆盖式音量条保持私有**（web 里也是 `VoiceMemberRow.tsx` 内部私有函数，用户 2026-09-21 拍板），
  三层 = 双色轨道 + 跳动条（`^0.4` 映射 / 80ms）+ slider（Flutter `Slider` + 自绘 14px thumb）。
- `widgets/voice_channels.dart`（**新**，2026-09-21，B1-1）：`AylaVoiceCardData` /
  `AylaVoiceChannelCard` / `AylaVoiceChannelList` / `AylaVoiceControls`。**没有新造材质**：
  卡面 = `GlassSurface`（`--glass-bg` + 1px 边 + blur24 sat1.4 + `--glass-shadow`，边色按
  hover/active 换）、交互 = `AylaCardInteraction`（hover 上浮 -2 / press .99 / 焦点环 /
  键盘可达）、标签 = `AylaCapsuleTag` + `AylaScrollingTags`、名称 = `AylaScrollingText`、
  入场 = `AylaRevealItem`、按钮 = `GlassButton`。
- `widgets/session_activity.dart`（**新**，2026-09-21，A5）：`AylaSessionActivityIndicator` +
  `AylaActivitySession`。**没有新造材质**：把手壳 = `AylaPressScale`（它就是 auroraqua 按钮组内的
  那档）、阴影 = `AylaGlassShadow.ring/.animatedRing`、玻璃 = `GlassConfig.backdropFilter(blurNav)`、
  图标 = `AylaIcon(iconMic/iconVideo, 20)`；文件里只有「球（不在按钮组，自带 1.08 + 辉光 150ms）」
  与「拖动几何」两部分是新的。结构照库内同类范本 `image_viewer.dart:709–811` 的
  `_ViewerCircleButton`（圆形钮 + hover/focus 辉光）。**返回 `Positioned`**（同 A4 约定）。

---

## 四、已知但未修的问题（待用户裁决）

### 4.1 `MediaSigner` 同源替换在 Flutter 侧无效（B4 遗留）
`core/media/media_signer.dart:193–198` 把签名 URL 换成 `/minio/...`（照搬 web 的 Vite 反代），但：
① 后端**没有** `/minio` 路由；② `ResourceImage` 用 `Image.network` 消费相对路径会被解析成
`file:///minio/...` → **所有签名媒体加载失败**。
**建议修法**：保留**绝对** presigned URL，Android 上把回环主机换成 `10.0.2.2`（同 `DioClient._baseUrl` 策略）。
影响面：帖子/评论图片、聊天媒体、视频源。用户当前态度：「minio 存储暂不做，留待后续重构」。

### 4.2 全库缺 `Tooltip`（悬停文字提示）
web 的图标钮普遍带 `title={label}`，Flutter 侧 `Tooltip` **零命中** → 悬停无提示。
属**全库统一项**（涉及所有 icon-btn），建议一次补齐，别只给单个组件加。

### 4.3 媒体/视频接线未完成（能力层已齐，剩页面接线）
- 「选文件 → 本地校验 → 三步上传 → 抓帧海报」→ 接到 `AylaPostEditor` / `AylaCommentComposer` 的注入口；
- `SignedVideo` 播放器接线（Windows 走 `media_kit`、其余 `video_player`）。
**这些属于页面层**，按用户要求排到页面阶段。

---

## 五、注意事项（环境与流程）

- **M0 遗留文件不要提交**：`flutter/lib/core/{api,net,ws}/`、`core/app_init.dart`、`lib/state/`、
  `lib/tool/`、`flutter/web/`、`devtools_options.yaml`、`docs/flutter/08-*.md`。
  既有状况：已提交的文件（如 `media_signer.dart`）**import 这些未跟踪文件** → **干净克隆无法编译**（已知）。
- **构建产物**：`build/windows/x64/runner/Release/ayla_flutter.exe`。
  ⚠️ **不要 `rm -rf build/windows`** —— 会连 `CMakeCache.txt` 一起删，导致安装前缀回落到
  `C:/Program Files/ayla_flutter` 并在 INSTALL 阶段因权限失败；该目录下还有两个**本机手工放置**的运行库
  （`mpv-dev-x86_64-20230924-git-652a1dd.7z` / `ANGLE.7z`，GitHub 不可达）。
- **预览宿主在 `/mnt/e`（drvfs）上增量编译可能不触发** → 画面陈旧。用户侧的解法：
  `bash tool/preview-up.sh --stop && rm -rf .widget_preview/build && bash tool/preview-up.sh`。
- **release 与预览观感有半像素差**（release 跑在 Windows 125% 缩放，DPR 1.25）→ **以 release 为准**校准。

---

## 六、关键决策存档（历次讨论的结论，避免重复推导）

### 6.1 媒体上传（B5-6，2026-09-20）

- **二进制上传走后端中转**：`PUT /api/v1/media/uploads/{upload_id}`（后端 `UploadBinaryView`：
  `request.stream` 流式分块写临时文件、边写边校验累计大小、超限 413、再 `put_stream` 到对象存储）。
  web 走的是 `presigned_url` 直打 MinIO + 把绝对 URL 换成 `/minio` 走 Vite 反代；
  **Flutter 侧没有这条反代**（后端 `apps/media/urls.py` 无 `/minio` 路由，也没有 Vite），
  故改用后端专门提供的直传端点（该端点**需要** Authorization，与预签名直传相反）。
- **ASGI 不解析 chunked**（本项目已知坑）：上传必须携带确定的 `Content-Length` → 实现以
  `Uint8List` 作请求体（dio 自动写入）；若要改流式，必须显式设置 `content-length` 头。
- **视频海报**：web 用 `<video>` + `<canvas>` 抓 0.1s 帧（宽边 640 / JPEG q85）。Flutter 没有
  等价物（抓帧依赖平台视频解码）→ `AylaMediaUploader.uploadPoster(mediaId, jpegBytes)` 已就位，
  抓帧由 `core/media/video_poster.dart`（`media_kit` 截帧）承担，失败静默。
- `AylaMediaKind` 从 `post.dart` 抽到 `core/models/media_kind.dart`（`post.dart` 继续 re-export，
  既有 import 不变），使 `core/media/*` 不再依赖帖子模型。

### 6.2 分享族（B5-5，2026-09-20）

- **复用边界**：`.share-sheet-card` 与 `.privacy-sheet-card` 规格同源（`--glass-bg-strong` /
  blur24 sat1.4 / radius-panel 20 / `--glass-shadow-modal` / 窄屏 60dvh 贴底），直接复用
  `AylaModalCard`（调用处传 `maxHeight: min(80vh,720)`、`narrowHeightFactor: 0.6`、
  `narrowRadius: 20`、`scrollable: false`）。宽屏入场（framer-motion `opacity 0→1 + scale .96→1
  + y 12→0`，250ms `--ease-out`）以新参数 `wideEntrance` 入库，默认关闭。
- **auroraqua 无覆写**：已 grep 全量确认 **没有** `.share-sheet-*` 选择器 → `share.css` 是唯一事实源。
- **选项卡不是滑动胶囊**：`share.css` 顶部注释写「单一选中胶囊 300ms 滑动」，但实现是每个 tab 自带
  `.is-active` 底（`--nav-active-bg` + `--glass-shadow-compact`，无边框、无扫光），tsx 也没有
  `.auroraqua-nav-highlight` 元素 → **按实现复刻，不按注释**。
- **关闭语义**：web `closedRef` → Flutter `_closed` 守卫；重复关闭只回调一次（已锁测试）。
- **画布里的弹层要用 `MediaQuery` 覆写视口**：断点/`60dvh` 读的是 `MediaQuery`，只用 `SizedBox`
  包住会仍按画布尺寸走宽屏形态。用 `MediaQuery.of(context).copyWith(size:)`（**不要**凭空 `MediaQueryData(size:)`）。
- **选项卡复用（用户点名）**：分享弹窗的双 tab 不自绘，用组件库 `AylaSegmentedTabs`，
  且用户最终指定**与消息中心统一用默认档**。`AylaSegmentedTabsVariant.shareSheet`（web 原版规格）
  **保留但当前未启用**，一行切换即可贴 web。
- **窄屏弹窗固定 60dvh 是 web 行为**：`.share-sheet-card.is-narrow { height: 60dvh; max-height: 60dvh }`
  + `.share-sheet-body { flex: 1; min-height: 0 }` → 条目少时下方留白**属于可滚动区**，不是多余容器；
  Flutter 侧用 `Expanded`（窄屏 fixed 高）/ `Flexible(loose)`（宽屏内容高）对齐 `flex: 1` 语义。

### 6.3 用户实测校准值（**优先于 web 原值**，已在代码注释标注）

| 项 | web 原值 | 用户校准值 | 出处 |
|---|---|---|---|
| 顶栏模块图标垂直偏移 | `translate: 0 -2px`（`shell.css:214–219`） | **`Offset(0, 1)`** | 逐次实机校准：下移2 → 上移1 → 下移0.5 → 上移0.5；release 与预览有半像素差，以 release 为准 |
| 群顶栏下拉退场（≥80px） | `setPullOffset(0)` 顶栏回原位 | **顶栏与内容一起继续下滑**，且**下滑封顶「贴底」不冲出屏幕** | `group_top_tabs.dart` 注释已标「偏离 web」 |
| 语音频道卡的 hover | `voice.css:543–545` 给语音卡**单独**加了 `:hover { border-color: rgba(157,191,230,.65) }` | **不复刻**：悬停不改边色，只走 auroraqua 卡片族统一悬停（上浮 -2px + `--glass-shadow-hover`；press `.99`） | 用户 2026-09-21 裁决「那是做错了，整个 web 端都是复用的同一个悬停」；`voice_channels.dart` 的 `body()` 注释已标「勿照 web 改回」 |
| 语音音量条条高（= 端帽 R 角上限） | `height: 4px`（`app.css:3024/3042`，端帽 `--radius-pill` 被浏览器夹到 **R=2 = 上限**） | **5px ⇒ R=2.5** | 用户 2026-09-21 报「两端不够圆角」⇒ 4px 条上 R 已到顶，加大 R 只能加厚；`voice_member_row.dart` 的 `barHeight` / `capRadius` 注释已标「用户实机校准，勿改回」 |

> ⚠️ 后续 agent：看到注释里写「偏离 web / 用户实机校准」时，**不要按 web 改回去**。

### 6.4 溢出父边界的子元素收不到指针 → 浮层必须走 Overlay（2026-09-21，A1 实测）

`AylaServerRail` 的悬停置顶面板在 web 里是 `position: absolute` **溢出 rail 右缘**
（`group.css` 594–612）。Flutter 侧照做成 `Stack(clipBehavior: Clip.none)` 后：
**面板画得出来、`tester.getRect` 位置也对，但点不动**（`tester.tap` 报
"would not hit test on the specified widget"）。

根因是 framework 语义、不是实现 bug：`RenderBox.hitTest` 先判断命中点是否落在
**自己 bounds 内**，父级 bounds 之外的点不会向下递归 → 溢出区的子元素永远收不到指针
（CSS 的溢出元素仍可接收事件，两者语义不同）。

结论与纪律：

- Flutter 侧**任何溢出宿主边界的交互内容都必须走 Overlay**：`aylaOverlayEntry` +
  `Overlay.of(context, rootOverlay: true).insert(entry)`；库内范本 `conversation_more_menu`、
  `top_nav` 的浮层。位置改用 `GlobalKey` + `localToGlobal(ancestor: overlayBox)` 实测；
- 只解决**绘制**不够（`clipBehavior: Clip.none` 只保证画得出来）；
- 定位类问题的验证口径：看 `tester.tap()` 的 hit-test 警告与
  `tester.hitTestOnBinding(offset).path`（**第一项即最深命中目标**），
  比"widget 在不在树上"可靠得多。

### 6.5 Flutter `BoxDecoration(border:)` 不占布局 ⇒ 需要 1px 内边距补位（2026-09-21，A1 实测）

CSS 的 `border` 参与盒模型（`box-sizing: border-box` 下 `.server-rail` 内容区 = 72 − 2×1 = 70），
Flutter 的 `BoxDecoration(border:)` **只画不占位** → 列表宽 72（多 2px）、行右缘 71→72，
浮层锚点整体偏 1px。修法：`GlassSurface(padding: EdgeInsets.all(railBorderWidth))`，
补位后行宽 70、行右缘 = 71（相对 rail 外框），与 web 逐值一致。
**凡"容器有 1px 边框且内部有按边距定位的内容"都要检查这一条。**

### 6.6 预览 cell 溢出：定位方法与两处既有样张修复（2026-09-21）

用户预览日志报 `A RenderFlex overflowed by 889 / 4 / 16 pixels`。**定位方法（可复用）**：
写一个**临时探针 testWidgets**，把每个 public `@Preview` 函数按它声明的 `size` 包进
`SizedBox(cell)` 渲染一次，再读 `tester.takeException()` —— 一次就能列出所有报错的 cell
（本次 28 个 cell 里 3 处异常、2 处根因：`imageViewer`、`topNavWide`；`serverRail` 与画布本身都 OK）。
跑完即删；别靠肉眼在预览器里找。

两处根因（**都是既有样张问题，与本轮 ServerRail 无关**）：

1. **`image_viewer` 样张的舞台按 MediaQuery 视口算**（`image_viewer.dart:304–306`：
   `stageMaxW = vw*0.92` 上限 1200、`stageMaxH = vh*0.82`），而样张把它放进固定 520×560 的盒子
   ⇒ 预览宿主/画布视口越大、舞台越高越宽 → 底部溢出 889px、右侧 4px×2。
   修法：盒子加大到 520×680，并用 `Builder + MediaQuery.copyWith(size: Size(520, 680))`
   **覆写局部视口**（与「画布里弹层必须覆写 MediaQuery」同一条纪律）。
2. **`top_nav` 宽屏 @Preview 的 cell 高 140 不够**（顶栏 64 + 上边距 12 + 说明行 + 按钮行 ≈ 155）
   → 底部溢出 15px；cell 改 `Size(1600, 220)`。

验收：`dart analyze lib` 零告警；探针 28/28 cell 全 OK；`flutter test --concurrency 1` → **223/223**。

### 6.7 hot reload 不支持「类字段集合变化」（2026-09-21）

把样张 `_ServerRailDemo` 从 StatelessWidget 改成 StatefulWidget 后，预览宿主报
`Const class cannot remove fields` → **hot reload 被拒**，只能 hot restart（日志后续的
`WipError -32000 Inspected target navigated or closed` 是目标失效后的连锁现象）。
凡「给已有样张加交互状态」「改类的继承/字段」都会触发；直接请用户 hot restart，别当 bug 查。

### 6.8 自建 CSS sticky + 动态裁剪（2026-09-21，A2a）

Flutter 没有 `position: sticky`（库内此前零 `CustomScrollView`/`SliverPersistentHeader` 用法）。
ChannelSidebar 需要「三个场景行依次吸顶 + 语音/直播同时吸底 + 下拉内容随列滚动」，
自建方案（已落地）：

1. **滚动内容里只放行占位**（`SizedBox(height: 40, key: slotKey)`），行本体画在顶层
   `Stack` 的 `Positioned` 上；
2. **位置公式**（等价 CSS 的 top/bottom 双约束，**top 优先**）：
   `y = max(top, min(natural − scroll, viewportH − 40 − bottom))`
   —— 内层 `min` 做底吸附、外层 `max` 保证视口不足时停在 top。
   ⚠️ 写成 `min(max(follow, top), maxY)` 是**错的**（视口不足时返回 maxY、越过 top）；
3. **几何靠实测**：`GlobalKey` + `localToGlobal(ancestor: 内容 Stack)` 取占位与下拉的
   自然坐标，postFrame 同步（值变化才 setState）；
4. **滚动偏移用 `ValueNotifier<double>` + `ValueListenableBuilder`** 驱动浮层重建，
   避免整树 setState；
5. **动态裁剪**（`useSidebarContentClip` 等价，paint-only 不改布局）：
   `start = max(0, headerBottom + gap, dropdownDrawnTop)`、
   `end = min(viewportH, nextHeaderTop − gap, dropdownDrawnBottom)`，
   再换算成 `ClipRect(clipper: 自定义 inset)` 的 top/bottom；
6. 点击场景项「滚到自身吸顶位」= `animateTo(natural − stickyTop)`（web 是 scrollTo smooth）。

### 6.9 Flutter 没有 CSS `overflow: hidden` 的静默裁剪（2026-09-21，A2a 两次踩到）

web 里「固定高度容器 + 超高内容」是**静默裁剪**；Flutter 的 `RenderFlex` 会直接报
`A RenderFlex overflowed by N pixels`（黄黑条纹、测试失败）。出现场景与修法：

- **语音房下拉折叠**：折叠高度 = `min(3, n) × 31 − 1`，内容仍是全部行
  → 必须 `SizedBox(height:) + ClipRect + OverflowBox(maxHeight: infinity)`
  （行的**禁用**语义同样要对齐 web 的 `disabled/tabIndex=-1/inert` → `IgnorePointer + ExcludeSemantics`）；
- **画布样张里的长文本**：不加宽度约束的 `Text` 会把 `Row` 撑出画布（实测 736 可用宽溢出 5.2px）
  → 样张说明文字一律包 `SizedBox(width: 组件宽 + margin)`。

### 6.10 依赖「实测几何」的布局必须给测量开**追踪窗口**（2026-09-21，A2a 用户报错位）

**现象**（用户报）：展开/收起、展开更多/收起时**父级选项卡不动、内容错位**。

**根因链**（用临时探针逐帧打印几何定位）：
1. 场景行位置依赖**实测**的占位坐标（[_stickyY] 的 natural）；
2. 测量只在 `build` 里排队 → 「点击 → build（此时布局仍是旧的、测得值没变）→ 不 setState
   → 不再 build → **不再测量**」；
3. 而高度动画由 `AnimatedSize` 自己的 ticker 驱动，**只重绘自身、不触发父级 build**
   ⇒ 整个动画期间的几何停留在旧值（实测：收起子群后语音行一直停在 178 不回收；
   展开更多 303→364 能生效纯属偶然，因为那次有别的 setState 带了一次重建）。

**修法**：`_scheduleMeasure({int ticks = 0})` + `_measureTicks` 追踪窗口 ——
在展开/收起与「展开更多/收起」的 onTap 里开 30 帧窗口，测量末尾若窗口未耗尽就继续排队；
动画每帧都测（值变就 setState），稳定后窗口耗尽自然收敛。

**推广**：任何「位置/尺寸靠 `GlobalKey` 实测、且依赖 `AnimatedSize`/`AnimatedContainer` 等
隐式动画结果」的组件，都要开追踪窗口；只靠 build 排队会在**缩小的方向**上失效。
调试手法：临时 `debugPrint` 在测量入口打印几何 + 探针 testWidgets 分阶段 dump 行位置 ——
一次就能看出「哪一帧开始没测」。

### 6.11 「同一帧」定位：把依赖布局结果的定位放进 `paint`（2026-09-21，A2a 用户报穿透）

**现象**（用户报，承接 §6.10）：会动了，但**响应比子列表慢** —— 子列表已到位、选项卡还在动，
出现穿透。要求「子列表和父级选项卡一块移动」。

**根因**：追踪窗口只保证「每帧都测」，但测量本身是 `postFrame`（layout **之后**）→ 行位置
永远用**上一帧**的几何；而 `AnimatedSize` 的本帧 layout 已经把子列表摆到位 ⇒ 恒定落后一帧。

**解法**：`_StickyRowLayer`（`SingleChildRenderObjectWidget` + `RenderProxyBox`）——
把行的定位从 build 搬到 **paint**：`paint` 里读占位相对视口的 `localToGlobal`（本帧 layout 已完成）
→ 算 clamp 后的 y → `context.paintChild(child, offset + y)`。**零滞后、与内容同帧。**

三个必须一起做的细节（每个都实测踩过）：

1. **`performLayout` 要自己写**：层用 `Positioned.fill` 铺满视口（命中测试要覆盖），
   但若沿用 ProxyBox 默认布局，tight 约束会把 40px 的行**拉伸到视口高**
   （实测 `child=Size(244,443)`）→ 位置与命中全错。正确：`size = constraints.biggest` +
   `child.layout(BoxConstraints.tightFor(width: size.width))`。
2. **必须覆盖 `applyPaintTransform`**（与 paint 用同一偏移）：否则 `localToGlobal`、
   命中测试、语义树、测试里的 `getRect`、可见性判断读到的还是**布局**坐标（y=0）。
3. **每次 widget 重建无条件 `markNeedsPaint()`**：层的位置取决于**外部**（滚动内容里的占位）
   的移动，占位移动不会让本层 markNeedsPaint（属性 setter 的 identical 比较会跳过）
   → 实测「内容在动、行不动」。

**验收判据**：动画每一帧「行 ↔ 其下拉首行」的距离恒定（实测全程 `gap=42`）。

### 6.12 预览宿主（Flutter web / CanvasKit）**不认系统字体** → 中文全方框（2026-09-21）

**现象**（用户截图）：预览里拉丁字正常（Ayla 对），**所有中文变成方框**。

**根因**：
1. 预览宿主 `.widget_preview/` 是一个**独立的 Flutter 工程**（自带 `pubspec.yaml` / `assets` /
   `preview_manifest.json`）—— **字体配置要看它，不是主工程** `Ayla/flutter/pubspec.yaml`
   （排查时先看这里，否则会一直在主工程里找错方向）；
2. 它跑的是 **Flutter web（CanvasKit）**，而 **CanvasKit 不认系统字体**：只能用**已注册的字体资源**；
3. `AylaFonts.cjkFallback` 里那四项（`PingFang SC` / `Hiragino Sans GB` / `Noto Sans SC` /
   `Microsoft YaHei`）**全是系统字体名** ⇒ 桌面端有效、**web 上一个都不生效** ⇒ 中文无字形 ⇒ 方框。

**修复**（只动预览宿主，已做）：
- 复制 `C:\Windows\Fonts\Noto Sans SC (TrueType).otf` →
  `.widget_preview/assets/fonts/NotoSansSC-Regular.otf`（**SIL OFL 许可、可自由分发**，优于微软字体；
  后者不可再分发，只能本机临时用）；
- 在 `.widget_preview/pubspec.yaml` 的 `fonts:` 里注册 **`family: Noto Sans SC`**
  —— 家族名必须与 `cjkFallback` 里某一项**完全一致**，否则匹配不上；
- 改完必须 `rm -rf .widget_preview/build` 再重启（pubspec 变了 → 字体资产要重新打包）；
- `tool/preview-up.sh` **不会**重写该 pubspec（已确认）⇒ 改动可保留。

**注意**：这是**预览环境**的限制；产品打包（Windows/Android/iOS）靠系统字体不受影响，
但**若将来发布 web 版**，主工程 `pubspec.yaml` 也必须注册 CJK 字体资源。

### 6.13 选中底**不许有过渡** + 零透明必须用**同色相**（2026-09-21，用户报「高亮块出现和消失有一段灰色过渡」）

**现象**：各种横向/纵向选项卡在选中态切换时，高亮块「出现/消失」拖着一层**灰**，拖沓且脏。

**两个机理叠加**：

1. **渐隐渐显**：web 里 `.has-auroraqua-highlight:is(.is-active, .active) { background: transparent }`
   是**瞬时**的 —— 选中瞬间底色直接交还给共享高亮块（挂载即出现、卸载即消失，
   `AuroraquaNavHighlight` 还带 `initial={false}`）；而组件库给这个底色变化套了
   `AnimatedContainer(200–300ms)` ⇒ 高亮块「渐显」出来 ⇒ 拖沓。
2. **透明黑插值**：`Colors.transparent` 是 `0x00000000`（**透明黑**）。Flutter 的 `Color.lerp`
   是**逐通道直插**（不按 alpha 加权），从 `.18 冰蓝` 插到透明黑的中途是 `rgb(118,120,121)`
   **中灰**；CSS 用 premultiplied alpha 插值（色相不变）故 web 无此现象。
   ⇒ 零透明一律写**同色相**透明（如 `ice500.withValues(alpha: 0)`）。

**统一口径**（已全库扫过一遍）：

- **选中态**（底色交还高亮块）⇒ `duration: active ? Duration.zero : <原时长>`；
- **hover 底色**保留 web 声明的过渡（导航组 300ms / 筛选 200ms / 图标钮 180ms）；
- **零透明**一律同色相（组件内可设常量，如 ChannelSidebar 的 `_SidebarZeroTint.hover`）。

**改动清单**：`AylaChannelSidebar`（场景项 + 子群/语音房/直播间行共 4 处 + 新增 `_SidebarZeroTint`）、
`AylaTopNav`（模块选项卡选中底 300ms→0；图标钮圆底零透明改同色相）、`AylaDirectoryFilters`
（选中态 200ms→0）。`AylaSegmentedTabs` / `AylaBottomTabs` / `AylaGroupTopTabs` 本来就无背景过渡
（不用改）；`AylaShareSheet` / `AylaPrivacySheet` / `AylaMenuItem` / 会话更多菜单早已用同色相。

### 6.14 同一行有多个浮层钮时，hover 必须**分成两层**（2026-09-21，A2 补修）

**现象**：聊天/语音/直播行右侧有 **＋ 与 三角**两个钮，悬停任一个**两个一起亮**（用户实报）。

**根因**：两个钮共用**行级** hover 身份（`_Selection('scene', 场景名)`）⇒ `hovered` 判定同时命中。

**web 的事实**（`group.css`）：
- 钮自身：`.channel-scene-voice-add:hover`（852）与 `.channel-scene-voice-toggle:hover`（883）
  —— **各自独立**；
- 行底色：`.channel-scene-row:has(.channel-scene-voice-add:hover) .channel-scene,
  .channel-scene-row:has(.channel-scene-voice-toggle:hover) .channel-scene { background: .18 }`（878–881）
  —— **任一钮 hover 都联动**。

⇒ 必须拆成**两个状态**：
- `_hoveredRow`（**行级** → 联动词条底色）；
- `_hoveredOverlay`（**钮自身** → 该钮底 `.3` + 主色图标），身份 = `overlay:<行kind>:<行id>:<slot>`，
  `slot ∈ { edit, add, toggle }`。

### 6.15 展开/打开态（`.is-open`）**不得**点亮底色（2026-09-21，A2 补修）

**现象**：三个场景行的三角展开键在**常态**下常驻灰蓝底 + 深色图标（用户实报「常态时这里不高亮的」）。

**根因**：`_buildOverlayButton` 的底色判定是 `hovered || activeState`，而三角键把**展开态**当成了
`activeState` 传进来 ⇒ 只要下拉展开就亮。

**web 的事实**：`.channel-scene-voice-toggle.is-open { transform: translateY(-50%) rotate(180deg) }`
（874–876）—— **只有 transform**；底色与字色**只有 `:hover` 才给**（883–886）。

⇒ 拆参数：`activeState`（`.is-active` → 点亮底色；**只有编辑笔**用）与
`rotated`（`.is-open` → **只旋转 180°**）。

> **通用教训**：CSS 里 `.is-open` / `.is-active` / `:hover` 是**三种不同状态**，
> 常被合并成一个 bool 传参 —— 迁移到 Flutter 时必须逐条对照选择器，
> 尤其「展开态只改 transform/几何、不改颜色」这一类。

### 6.16 Dart `List.sort` 不是稳定排序（2026-09-21，A2a）

web `sortSubgroupsByActivity` 依赖「并列保持列表原序」；Dart 的 `List.sort` 是**不稳定**的
→ 必须显式带原索引做二级比较（`return a - b`），否则 UI 顺序会漂移。
同类：任何从 web 移植的 `.sort()` 都要检查稳定性假设。

### 6.17 频道侧栏重做（A2）实现期新踩的 5 个坑（2026-09-21，供后续 agent 直接复用）

第一版失败点（坑 1–9）见 `15-重做频道侧栏-启动提示词.md`；**重做版新踩**的是下面这些：

1. **`localToGlobal` 不能在 `performLayout` 里调**：sticky 行的位置要读兄弟子树（占位）的坐标，
   而 layout 期间兄弟可能尚未布局 → `RenderBox.size accessed beyond the scope of resize`
   （实测：行层 `performLayout` 里算 y 直接崩）。**位置一律放 `paint`**（本帧 layout 已完成），
   `performLayout` 只负责 `size = constraints.biggest` + `child.layout(tightFor(...))`。
2. **裁剪层的坐标基准必须统一**：下拉裁剪层挂在滚动内容的 `Column` 里，`paint(offset)` 的
   `offset.dy` 是**相对 Column** 的坐标，而 `rowTop()`/视口 rect 是**相对卡片**的坐标 →
   混用会把 `topInset` 算成 63px（实测：下拉被裁到只剩 4px，点击全失效，`tester.tap` 报
   "would not hit test"）。修法：`localToGlobal(Offset.zero, ancestor: cardBox).dy` 统一基准。
3. **裁剪必须同时作用于命中**：web 的 `clip-path` 会裁掉指针命中区（被吸顶行盖住的内容点不到）；
   只做绘制裁剪会让「已被盖住」的按钮继续吃掉点击。`_DropdownClipRender.hitTestChildren`
   要按裁剪矩形过滤。**几何未就绪（`rowTop() == null`）时不裁剪** —— 按 `selfTop` 兜底会凭空
   裁掉顶部 44px，把首屏内容一起挡掉。
4. **GlobalKey 指向的行会在同帧失活**：收起子群会移除追加段里的行 → 该 key 的 element 变
   inactive/DEFUNCT，`findRenderObject()` 抛 "Cannot get renderObject of inactive element"
   （实测：胶囊层 paint 崩）。统一走 `_renderObjectOf(key)`（`mounted` + `try/catch` 双保险）。
5. **自绘浮层必须显式标脏**：自绘 RenderObject 的属性 setter 不会自动重绘/重排
   （`_StickyRowRender` / `_DropdownClipRender` 的位置与裁剪都依赖外部几何）→ 属性变化后
   要 `markNeedsPaint()`；另外首次 layout 完成后补一次（首帧 paint 可能早于兄弟子树 layout）。
6. **共享胶囊一律照范本，不要自研定位层**（用户 2026-09-21 点名）：第一版重做时我写了
   paint 定位 + 时间戳插值的自研层，被指出「应该参考组件库现有成功范例，或复用」。
   正解 = `AylaDirectoryFilters` 555–587 那套：**容器级单实例 `AylaNavHighlight`**
   + `GlobalKey` **实测选中按钮矩形**（`_measureCapsule()`，每次 build 排队 + 滚动/动画
   期间每帧重排）+ `AnimatedPositioned`（300ms `--auroraqua-ease-out`，等价 Framer `layoutId`
   的跨项迁移）+ 外层 `AnimatedScale(.98, 200ms)` 同步按压。**尺寸也来自实测**（不按组推算）。
7. **画布/预览样张必须可交互**（用户 2026-09-21 点名「没做交互」「父级/二级选项卡都要交互」）：
   组件是受控的（`activeScene`/`activeSubgroupId`/… 都是外部传入）→ 样张只摆固定参数就是
   **死的**。正确做法：样张内嵌一个 `StatefulWidget` 宿主自持选中状态并在回调里 `setState`
   （`_SidebarDemo`），这样预览/画布里点一级场景项、点子群/语音房/直播间行、点三角展开、
   点笔进编辑态都能真实响应（高亮迁移 / 扫光 / 按压全部可见）。

**测试侧**同样两条：`previewScope` 的 `Overlay(initialEntries:)` 只在首次创建生效 →
同一 `testWidgets` 里改参数必须用 `StatefulBuilder`（否则读到旧参数）；
`SemanticsHandle` 必须在测试体内 dispose（`addTearDown` 晚于框架校验）。

### 6.18 A3 CreateSheet：容器三件套 + `sp4 = 16` 陷阱（2026-09-21）

`layout/CreateSheet.tsx`（61 行）本体就是「遮罩 + 卡片 + 标题行」，Flutter 侧**一件材质都没新写**：

| web 事实源 | Flutter 承载 |
|---|---|
| `.create-sheet-overlay`（`private.css:187–197`） | `AylaModalOverlay(padding: **AylaSpacing.sp4**)` |
| `.create-sheet-card`（`199–212`） | `AylaModalCard(maxHeight: vh×0.8, padding: fromLTRB(sp4, sp4, sp4, sp4 + safeArea.bottom), scrollable: true)` |
| `.create-sheet-head` + `.create-sheet-title` + `.icon-btn-40`（`214–226` / tsx 50–55） | 新公共件 `AylaSheetHead(closeIconSize: 20)` |
| tsx 30–36 ESC | `Shortcuts + Actions(DismissIntent) + FocusScope(autofocus)`（范本 `share.dart`） |
| tsx 41–43 点遮罩关闭 | `AylaModalOverlay(onDismiss:)`（卡内点击已被拦住） |
| tsx 47–48 `role=dialog aria-label` | `Semantics(scopesRoute: true, explicitChildNodes: true, label: title)` |
| `241–258` 窄屏贴底形态 | `AylaModalCard` 既有实现（radius 24 / 只留 top border / 250ms 上滑） |

**三条硬事实（后续 agent 直接用）**：

1. **`--sp-4` = 16px，不是 24**。`.create-sheet-overlay` 与 `.create-sheet-card` 的 padding 都是 16；
   而 `AylaModalOverlay.padding` 的**默认值是 24**（privacy 那一档）→ 必须显式传 `AylaSpacing.sp4`，
   漏了会让遮罩留白整档偏大（`AylaModalCard.maxHeight` 默认已是 `vh*0.8` = 80vh，无需再传）。
2. **关闭钮图标尺寸逐处不同**：`CreateSheet.tsx:53` = **20**、`ConfirmDialog.tsx:85` = **18**
   （`PrivacySheet.tsx:189` 亦 18）→ `AylaSheetHead.closeIconSize` 设为**必填**，禁止凭印象统一。
3. **`.icon-btn-40` 完整事实链**（零改动复用 `AylaIconButton`）：`home.css:121–134`（40×40 / pill /
   hover 底 `rgba(157,191,230,.18)` / `transition: background 180ms`）+ `auroraqua.css:125–139`
   （`--glass-bg` + 1px 亮边 + `--glass-shadow-button` + **`blur(8px)` 无 saturate**）+
   `base.css:343 button:disabled { opacity: .55 }`；**扫光不在弹层关闭钮上**
   （只在 `.narrow-topbar-more >` / `.top-nav-more >` 两处，`auroraqua.css:142`）→ `sweep: false`。

**同批修正的既存偏差**：ConfirmDialog / PrivacySheet 的关闭钮此前是**手搓 40 盒 + Material
`Icons.close` + 次要色**（`dialogs.dart` / `privacy_sheet.dart`），与 web 的
`button.icon-btn-40` + `IconClose` 不符（丢掉玻璃材质 / hover / 按压，颜色掉档）→ 已按 web 改；
`PrivacySheet` 顺带补 `.privacy-sheet-head { gap: var(--sp-3) }`（`profile.css:673`）。

**登记（不在 A3 范围，勿当漏项）**：

- `private.css:229–238` 是**卡片作用域内的表单样式**（`.create-sheet-card .voice-create-input` /
  `.game-room-create .field` 宽 100% + `margin-bottom sp3`；`.btn-primary:not(.post-editor-submit)`
  宽 100% 居中）→ 归表单组件自身（VoiceChannelCreate = B1、GameRoomCreate = B4）；
  Flutter 无后代选择器，容器不替它们定宽；
- `z-index: 70`：Flutter 侧由 Overlay/Stack 的插入顺序决定（web 注释：抽屉层 70–80 低于
  `.create-sheet-overlay(70)`，二次确认要盖在其上，见 `group.css:2134`）→ 页面层接线时定序；
- tsx 的 `className` 唯一用例是 `live-viewer-sheet-card`（`live.css` 窄屏 60vh 名单弹层）→
  等价映射为 `maxHeight` / `narrowHeightFactor` / `scrollable` / `narrowRadius` 透传
  （不发明 web 没有的语义）；
- 两个**未动**的既有观察：① `post_editor.dart` 的样张用
  `GlassCard(strong: true, radius: rPanel, shadow: modal)` **手工模拟** `.create-sheet-card`
  （CreateSheet 落地后可换真实容器，但它属已验收样张）；② `AylaModalCard` 的宽度是 `maxWidth`
  约束（内容窄则卡片跟着窄），web 是 `width: min(480px, 100%)` 固定宽 ——
  真实用例（表单 / 编辑器都是 width:100%）下等价，若要严格对齐需动被四处共用的 `AylaModalCard`。

### 6.19 A4 右下浮层按钮族：定位数值 + 三个 Flutter 语义坑（2026-09-21）

`layout/{CornerFabStack,RefreshFab,ScrollTopFab,QuickMessageFab}.tsx` + `shell.css` 423–456 / 679–787。
四件一批，**没有新造材质**：44px 玻璃钮 = `AylaCornerFab`、56px 消息钮 = `AylaMessageFab`、
徽标 = `TabBadge(tab 档)`；新文件只补「定位档 / 旋转 / 浮入淡出 / 半贴位移 / 滚动判定」。

**定位数值（逐条抄自 CSS，别改）**：

| 选择器 | 值 | Flutter |
|---|---|---|
| `.corner-fab-stack` | `fixed right calc(32px + (56-44)/2)` = **38**；`bottom calc(32px + 56px + sp3)` = **100**；`gap sp3` = 12；`align-items: flex-end`；`pointer-events: none` | `Positioned(right: 38, bottom: 100)` + `Column(spacing: 12, crossAxisAlignment: end)`；容器不吃指针由裸 `Column` 天然成立（**别**加 `IgnorePointer`，会连子项一起忽略） |
| `.corner-fab-refresh.is-bottom-left` | `fixed left 32 / bottom 32`（**无 safe-area**） | `Positioned(left: 32, bottom: 32)` |
| `.corner-fab-scroll-top.is-narrow` | `right 16`；`bottom calc(64px + safe-area + sp3)` = 76 + safe | `Positioned(right: 16, bottom: 76 + MediaQuery.padding.bottom)` |
| …`.is-narrow.is-stacked` | `right calc(16px + 6px)` = **22**；`bottom … + 56 + 12` = **144 + safe** | `Positioned(right: 22, bottom: 144 + safe)` |
| `.quick-message-fab.is-collapsed` | `translateX(-44px)`（仅露 28px 右半） | `TweenAnimationBuilder` + `Transform.translate(-44 × v, 0)`（精确像素，不用 `AnimatedSlide`） |
| `ayla-loading-spin` | `var(--loading-spin-duration)` = **800ms linear infinite** | `RotationTransition`（`AylaDurations.spin`） |

**回顶钮判定**（tsx:18–21 / 43–49）：滚动源必须是**主滚动容器**（`scrollHeight > clientHeight`
且 `clientHeight ≥ 40% window.innerHeight`）且 `scrollTop > window.innerHeight` 才浮入。
Flutter 等价：`viewportDimension` ↔ `clientHeight`、`MediaQuery.size.height` ↔ `window.innerHeight`；
滚动源取「注入 `controller` 优先，否则最近祖先 `Scrollable`」（web 用 document capture 监听任意容器，
Flutter 无全局 scroll 事件 → 两种装配都要覆盖）。`behavior: "smooth"` 浏览器无显式时长 →
与库内 ChannelSidebar 口径统一 **300ms ease-out**；reduced-motion → `jumpTo(0)`（= `"auto"`）。

**无回调 ≠ 禁用**：`RefreshFab` 的 `if (!fn) return` 是「点击无动作」，按钮外观 / hover 照常 →
**不能**传 `onPressed: null`（那会走 `.55` 禁用外观）。

#### 三个 Flutter 语义坑（本次实测，后续同类组件直接复用）

1. **`ScrollPosition.viewportDimension` / `.pixels` 在首帧 layout 之前是 null**——
   在 `didChangeDependencies`（或任何 layout 前）读会抛
   `Null check operator used on a null value`（栈指向 `scroll_position.dart` 的 getter）。
   → 用 `hasViewportDimension` / `hasPixels` 守卫，并把首轮计算排到 **postFrame**。
2. **`didChangeDependencies` 里 `setState` 会让 `_MediaQueryFromView` 的依赖断言失败**
   （实测 `'check that it really is our descendant'`）——首帧 `MediaQuery` 正在 `notifyClients`。
   → 首轮不要 setState，统一 `addPostFrameCallback`。
3. **`ScrollController.attach()` 不会 `notifyListeners`**（它只把 `notifyListeners` 挂到 position 上）
   → 首帧列表往往尚未 attach、`positions` 为空，滚动源绑不上 → build 里补一次 postFrame
   兜底绑定（`_bindSource` 幂等，绑上后不再排队）。

#### 测试侧三条

- `find.ancestor(of: X, matching: find.byType(Positioned))` **找不到** `StatelessWidget` build 返回的
  `Positioned`（build 结果是它的**子树** → 用 `descendant`）；而 `descendant` 又会连带命中
  `AylaCornerFab` 内部的 `Positioned.fill` → 用 `find.byWidgetPredicate` 锁「属性唯一」或
  「`child` 类型」最稳（`w is IgnorePointer && w.child is ExcludeSemantics`）。
- 同一个 `testWidgets` 里第二次 `pumpWidget` 改参数**不生效**（`previewScope` 的
  `Overlay(initialEntries:)` 只在首次创建生效）→ 换参数一律另开测试。
- 计时/半贴类断言用 `await tester.pump()`（**不带时长**）：既重建状态、又不推进时间；
  带时长会把短延时的计时器推进到再次触发（第一版就这么被坑）。

### 6.20 PostCard 头部折行修复：CSS flex 的 `min-width: auto` + `overflow` 语义差（2026-09-21）

**现象**：`post_card_test` 的「常规卡（360）+ 2 标签 → 时间与标签同排单行」长期失败
（`Wrap` 实测高 46，期望 < 30）。**不是新引入的回归**，是既有失败。

**探针实测**（卡片 360 / 内容区 328）：
- 时间 = `aylaPostCardTime(...)` = **「2026/9/20」108px**（>24h 走 `toLocaleDateString`，与 web 同源）；
- 标签「公开」38 +「深夜电台」60；
- ⇒ 右侧组自然宽 = 108 + sp3 12 + 38 + sp1 4 + 60 = **222**。

**根因**：`post_card.dart` 里 `nickReserve = 64`（注释写「昵称最少保留 4 个汉字」）把右侧组上限压到
`328 − 53(头像+halo+gap) − 12 − 64 = 199 < 222` ⇒ 标签折行。

**web 的真实语义**（本轮关键教训）：

```css
.post-card-nick { overflow: hidden; text-overflow: ellipsis; white-space: nowrap }
```

CSS flex 规范：**`overflow` 非 `visible` 的 flex item，其 `min-width: auto` 解析为 0**
⇒ 昵称可以被压缩到 0（ellipsis 兜底），而 `.post-card-tags`（无 `overflow:hidden`）的
`min-width: auto` = min-content = 最长标签宽 ⇒ **要保住完整宽度的是标签组，不是昵称**。
Flutter 没有这套自动最小尺寸，必须手工把「该保谁」写对。

**修法**：`nickReserve` 64 → **0**（昵称仍由 `Flexible` + `TextOverflow.ellipsis` 兜底 ⇒ 不会溢出）。
复验：`Wrap` 高 **21**（单行）、两标签 y 一致、右缘贴齐内容右缘；窄卡（220）时上限 123 ⇒
标签组内部兜底折行且不溢出（既有「极端窄卡 + 4 长标签」测试仍通过）。

**基线**：全量 `flutter test --concurrency 1` → **300 通过 / 0 失败（全绿）**。

> 同类排查提示：凡「CSS flex 一行里有一项带 `overflow:hidden` + 一项带 `flex-wrap`」的结构，
> 折行优先级都如上 —— 先压带 `overflow:hidden` 的（可到 0），最后才让可 wrap 的那组折行。

### 6.21 A5 会话活动悬浮球：三条「照实渲染」拍板 + 三个新坑（2026-09-21）

新文件 `widgets/session_activity.dart`（`AylaSessionActivityIndicator` + `AylaActivitySession`），
事实源 `layout/SessionActivityIndicator.tsx`（181 行）/ `shell.css:458–575` / `:651–660` /
`auroraqua.css:54–94`、`:655–677` / `base.css:333–346`、`:366–370`。

#### 三条判断（用户 2026-09-21 逐条拍板，**勿照 CSS 字面改回**）

1. **把手 hover/focus 底色 = 透明**：`.session-activity-toggle:hover, :focus-visible {
   background: var(--glass-bg-hover) }`（shell.css 550–554）里的 `--glass-bg-hover`
   **在 web 全历史从未定义** —— `git log -S'--glass-bg-hover:'` 零命中，`web/src/styles/`
   与 `dist/assets/index-*.css` 里都只有 4 处**引用**（`posts.css:334`、`shell.css:552`、
   `voice.css:229/249` 同病）。按 CSS 变量规范属「invalid at computed-value time」
   ⇒ 该 `background` **简写**整体回落到初始值 = **完全透明**（1px 亮边 / blur / 阴影全部保留，
   只有底色没了，字色转 `--text-primary`）。Flutter 侧一比一复刻**实渲染结果**，
   且零透明写**同色相**（`glassBgStrong.withValues(alpha: 0)`，见 §6.13）。
2. **球不加背板模糊**：`.session-activity-ball.is-voice/.is-live` 的底色是不透明的
   `--sakura-100` / `--ice-300` ⇒ CSS 声明的 `blur(18px) saturate(1.4)` 被自身底色完全盖住、
   视觉恒为零 → 省掉那次离屏层；而**把手**是 `.78` 半透明玻璃 ⇒ **必须**保留 blur18 sat1.4
   （测试断言整个组件只有 1 个 `BackdropFilter`）。
3. **焦点环沿用 `AylaPressScale`**：库内把 `:focus-visible` 的
   `outline: 2px --glow-500; outline-offset: 2px` 实现为**内嵌** `Container(padding 2 + border 2)`
   ⇒ 键盘聚焦时元素会**长大 4px**（web 的 outline 不占布局）。属已知库内差异，
   本轮**记录不改**（改动会波及其他全部按钮的验收基线）。

#### 层叠与 reduced-motion（已逐条确认）

- `auroraqua.css:62/80/92` 的按钮组**含** `.session-activity-toggle` ⇒ 200ms `--auroraqua-ease`
  + `:hover{scale:1.02}` + `:active{scale:.98}`（覆盖 shell.css:543 的 `background 150ms`）；
  **不含** `.session-activity-ball` ⇒ 球只有自己的 `scale(1.08)` + 辉光，且过渡是 **150ms**
  `--ease-out`（既不是 180 也不是 200）。
- **reduced-motion 的关闭名单也不含把手本体**：shell.css 569–575 只列
  `.session-activity-group` / `-toggle-icon` / `-ball`，auroraqua 的 reduced-motion 段只清
  `translate`/`scale` ⇒ 把手**底色与字色的 200ms 过渡照旧保留**，被关掉的只是缩放。
  （首版把把手底色过渡也在 reduced-motion 下归零，自检时对照 569–575 逐行修正。）

#### 拖动：为什么必须走原始指针事件

web `DRAG_THRESHOLD = 5`（tsx 9），而 Flutter 手势识别器要等 `kTouchSlop`（18px）才回调
⇒ 用 [`Listener`] 原始指针做几何（位移 ≥5px 即移动整组、`clamp(8, 视口高-44-8)`），
另挂一个**回调留空的** `GestureDetector(onVerticalDrag*)` 只用来赢下竞技场 ——
它等价 web 的 `touch-action: none`（拖把手时页面不滚动），已用「背后放 ListView + 触摸拖动」
的定向测试锁住 `controller.offset == 0`。

**抑制标记必须在新交互的 pointerdown 清零**：web 的合成 click 必然到达、标记必被消费；
Flutter 位移超过 tap slop 时**根本不产生 tap** ⇒ 若不清零，标记会留给下一次真实点击。
两种情形各有一条测试（10px：仍产生 tap → 被抑制；60px：不产生 tap → 下一次点击照常收起）。

#### ⚠️⚠️ 交付后用户实测报出的实现 bug：收起不是「动画」而是「跳变」（2026-09-21）

**现象**（用户原话「展开收起是有动画的，仔细看看 web 端代码」）：点把手后**球瞬间消失**，
只有整组在滑。

**探针中间帧实测**（`tester.getRect` + `FadeTransition.opacity.value`，200ms 动画每 50ms 采样）：

| t | 组（把手右缘） | 球自身位移 | 球透明度 |
|---|---|---|---|
| 0 | 976 | 0 | 1.0 |
| 50 | 990.3 | **64（已到终值 ✗）** | **0.0（已到终值 ✗）** |
| 200 | 1000 | 64 | 0.0 |

**根因**：`_ActivityBall` 把 `ExcludeFocus/ExcludeSemantics/IgnorePointer` 写成
「`collapsed` 时才包一层」——翻转时**根 widget 类型变了**（`Focus` ↔ `ExcludeFocus`），
Flutter 于是重建整棵子树 ⇒ `TweenAnimationBuilder` 在 `initState` 里把 `begin` 设成 `end`
（= 立即到位）。组位移不受影响（它的 Element 没被重建），所以**只断言终值的测试全绿**。

**修法**：三个包装器**恒在**，只翻转标志位（`excluding:` / `ignoring:`）⇒ 子树 Element 稳定，
隐式动画（`TweenAnimationBuilder` / `AnimatedOpacity` / `AnimatedRotation` / `AnimatedScale`）全部恢复。

**顺带修正的时长**：球的 `transform` 两个方向**不同**——`.is-collapsed` 规则是 200ms、
基础规则是 **150ms**（shell.css 495–496 vs 500–506）；`opacity` 两向都是 200ms
⇒ 位移与透明度必须分成两个动画宿主。首版两个方向都按 200ms 做，已改。

**纪律（本事故的通用教训）**：**任何依赖隐式动画的子树，其 Element 结构必须跨状态稳定**；
需要按状态增减包装器时，改成「恒在 + 标志位」。验证动画必须量**中间帧**
（`getRect` 的像素 + `FadeTransition.opacity.value` 的活值——`AnimatedOpacity.opacity`
是**目标值**，读它在动画中途也永远是终点，会把「有动画」误判成「跳变」）。
两条回归测试已入 `session_activity_test.dart`（收起三路中间帧 / 展开 transform 150ms 与 opacity 200ms）。

#### 三个新坑（供后续 agent 直接复用）
1. **`previewScope` 的 `Overlay(initialEntries:)` 只在首次创建生效** —— §6.19 已记过一次，
   本轮又踩：同一 `testWidgets` 里第二次 `pumpWidget` 换 props / 换视口**都不生效**
   （表现为「旧球还在」「视口还是窄屏」）⇒ 一律拆成独立用例。
2. **`previewTheme` 的 `Material` 自带一层 `AnimatedDefaultTextStyle`** ⇒
   `find.ancestor(of: find.text('›'), matching: find.byType(AnimatedDefaultTextStyle))`
   会 "Too many elements"；要收紧成 `find.descendant(of: 把手面, ...)`。
   同类：`AnimatedContainer` **没有** `width`/`height` getter（折进了 `constraints`）。
3. **`Semantics(label:)` 不会覆盖子 `Text` 的标签**（Flutter 会拼成 `收起媒体控制\n›`），
   而 web 的 `aria-label` 是**覆盖**内容 ⇒ 装饰字形要 `ExcludeSemantics`；
   `SemanticsNode.hasFlag` 在本 SDK 已弃用 → 用 `flagsCollection.isExpanded.toBoolOrNull()`。

#### 库内档位扩展（本轮唯一动到共享件的地方）

`AylaGlassShadow.animatedRing` 新增可选 `curve`（默认 `AylaCurves.auroraqua`，**既有调用点不变**）：
球的 `box-shadow` 过渡是 `150ms --ease-out`（shell.css 495–496，且球不在按钮组内），
与按钮组的 `--auroraqua-ease` 不同。

#### 页面层接线项（本件**不做**，与 A4 同口径）

- `useAuthStore`/`useSessionActivityStore`/`useLocation` 的三段解析（roomId → 会话匹配
  `stored.sessionId === String(roomId)`，不匹配时兜底 `title` 语音房/直播间 +
  `sourceRoute` `/voice/:id`、`/live/start/:id`，tsx 41–68）与 `onVoiceRoom` / `onLiveConsole`
  的 pathname 判定（tsx 71–82）；
- 层序 `z-index: 55`（页面内容之上、直播小窗 60 / 弹层 70–80 之下）；
- `design.md:485` 的「小窗激活时隐藏直播球」在 tsx **未实现** ⇒ 同样留待页面层。

#### 未完成项（登记，交用户裁决）

两球的 `title={session.title}`（浏览器原生 tooltip）属全库 `Tooltip` 统一项（§4.2：
一次补齐、别只给单个组件加）⇒ 本件不加。

**验收**：`dart analyze lib` 零告警；`session_activity_test` **32/32**；
全量 `flutter test --concurrency 1` → **332 通过 / 0 失败**。

### 6.22 B1-1 voice 域第一批（卡片 / 网格列表 / 控制条）：三处上下文同源 + 三条拍板 + 一个新坑（2026-09-21）

新文件 `widgets/voice_channels.dart`；事实源 `VoiceChannelCard.tsx`(47) /
`VoiceChannelList.tsx`(43) / `VoiceControls.tsx`(31) + `app.css:2811–2832 / 2910–2915 /
3099–3120` + `voice.css:471–485 / 505–628 / 647–659 / 690–789` + `typed-result-cards.css:46–140`
+ `auroraqua.css:28–52`。

#### 层叠关键结论（防后人误加档位）

`.voice-hub`（voice.css 526–628）/ `.group-voice`（697–789）/ `.typed-result-card`
（typed-result-cards.css 46–140）三处的**卡片视觉声明逐字相同**（column · `align-items: stretch` ·
gap 8 · padding 12 · radius 16 · `--glass-bg` · 1px 边 · `--glass-shadow` · `blur24 sat1.4`）
⇒ **只有一个竖排形态**，差异全在**容器网格**：`.voice-hub` 2 列 →（≥769）3 列 →（≥1440）4 列 +
padding 12/16；`.group-voice` **恒 2 列** + padding 0（那两条媒体查询只作用于 `.voice-hub`）；
typed-result 单列。故 Flutter 侧用 `columns` / `padding` 参数表达，**不设 variant**。
`app.css:2817` 的**横排**基础卡在真实渲染中从不出现（三个使用点都在上述容器内）⇒ 不实现。

#### 三条拍板（用户 2026-09-21 逐条确认）

1. **`.voice-leave-btn` 加 `GlassButtonVariant.outlineDestructive` 档**（不手搓按钮）；
2. **卡片焦点环走 `AylaCardInteraction.focusRingColor`**（`--ice-500`，画在形状外，
   **不占布局** —— 顺带把键盘可达性 `Enter`/`Space` 也收进卡片壳，web tsx 21–24 同义）；
3. **来源标签 `max-width: 12ch` 按 `ch` 语义实测**（同字体 Fredoka 11 / ls .8 下量 `0` 宽 ×12），
   不写死像素。

#### 一个新坑：`IntrinsicHeight` 与 `LayoutBuilder` 不兼容

web 的网格是 CSS grid（默认 `align-items: stretch`）⇒ **同一行卡片等高**，卡内
`margin-top: auto` 把 foot 压到底。Flutter 侧本想用 `IntrinsicHeight` 表达，但卡片里的
`GlassButton` 内部有 `LayoutBuilder`，而 **`LayoutBuilder` 不支持 intrinsics**
（`computeMaxIntrinsicHeight` 抛错）⇒ 改为**预留 owner 行高度**（`12 × 1.55`）表达同行等高。
**已登记差异**：整行都没有 owner 时，本实现比 web 多留一行空白（web 那行会更矮）。
凡「卡片里含 `GlassButton`/`LayoutBuilder` 又要同行等高」的场景都适用这条。

#### 复用清单

`GlassSurface`（`borderOverride` 换 hover/active 边色——单一描边层，不叠第二层）·
`AylaCardInteraction` · `AylaCapsuleTag` · `AylaScrollingTags` · `AylaScrollingText` ·
`AylaRevealItem`（`staggerDelay` 默认 gap **50ms**，cap 300——不是群卡网格的 80ms）·
`GlassButton` · `aylaVisibilityLabels()`（`post.dart` re-export，web `getVisibilityLabels` 同源）。

#### 测试侧再次踩到同一个坑

`previewScope` 的 `Overlay(initialEntries:)` **只在首次创建生效** ⇒ 同一用例里第二次
`pumpWidget` 换参数/换视口都读旧树（§6.19 / §6.21 已记过两次，本轮第三次）。
`voice_channels_test.dart` 里统一用**树内 `StatefulBuilder`** 驱动多状态（新助手 `stateful()`），
需要换视口的用例则拆成独立用例。

#### 接线项（本件不做）

`VoiceChannelCreate` / 两个 Panel / `VoiceMemberRow` / `VoiceRoomBody` 属 B1 后续批；
收藏槽由页面层注入 `AylaFavoriteButton(compact: true, ...)`（web 是卡片内部自取 store）。

#### 交付后用户裁决：语音卡的 hover 按「统一卡片悬停」（2026-09-21）

web 除了 auroraqua 的卡片族悬停（`28–52`：`translate: 0 -2px` + `box-shadow: --glass-shadow-hover`）
之外，还给语音卡**单独**写了一条 `.voice-hub .voice-channel-card:hover { border-color:
rgba(157,191,230,.65) }`（`voice.css:543–545`，`.group-voice` 与 typed-result 同款）。
首版照抄了这条 ✗ —— 用户判定：**那是 web 端的错误**，整个 web 的卡片本应复用同一套悬停。

⇒ Flutter 侧改为：**悬停不改边色**（任何悬停态都保持 `--glass-border`），只保留统一悬停的
两半——`AylaCardInteraction` 的上浮 -2px（300ms）与 `GlassSurface(shadow: hovered ?
AylaShadows.glassHover : AylaShadows.glass)`；`press` 的 `scale .99` 仍由 `AylaCardInteraction`
承载（卡片族共用，**不是**悬停态）。`.active`（当前频道 → `--indigo-700`）是**状态**不是悬停，保留。

回归测试两条：hover（边色不变 + 换 hover 阴影 + 上浮 2px）、press（scale .99 出现/消失）。

#### 交付后用户指出的两处（2026-09-21，均已修）

1. **描边 `--indigo-700` 是 `.mine`（我在其中）卡特有**（`app.css:2832`
   `.voice-channel-card.mine { border-color: var(--indigo-700) }`）——首版只实现了
   `.active`（`voice.css:557–559`）✗ 漏了基类那条。现按 **`active || mine`** 实现（两者语义不同、
   真实数据里通常重合）；
2. **「我在其中」同时最多一张**（用户原话「只能有一个'我在其中'」）——首版样张里有**两张**
   `mine: true` 的卡，且描边落在**非 mine** 的卡上 ✗。现改为：样张只保留一张 mine 卡，
   **并让它同时是 `currentChannelId`**（描边落在同一张上）；`browsing` 档改用普通卡演示
   （web 里 `mine + browsing` 也走按钮、不显示「我在其中」，样张里注明而不再造第二张 mine 卡）。

回归测试新增两条：`mine` 单卡的描边 + 「列表里只有一张『我在其中』⇒ 只有它带描边」。

**验收**：`dart analyze lib` 零告警；`voice_channels_test` **29/29**；
全量 `flutter test --concurrency 1` → **433 通过 / 0 失败**。

### 6.23 B1-2 voice 域第二批：成员行 + 覆盖式音量条（2026-09-21）

新文件 `widgets/voice_member_row.dart`；事实源 `VoiceMemberRow.tsx`（219 行全文，含文件内私有
`VoiceVolumeMeter` 40–90）+ `app.css:2917–3095` + `auroraqua.css:59/77/89/664`。

#### 音量条（用户点名问过的那件）

- 容器 **90×20**；三层（下 → 上）：**轨道**（`linear-gradient(90deg, --indigo-700 0→--fill,
  rgba(189,212,233,.55) --fill→100)`，`--fill` = 设定音量百分比）→ **跳动条**（宽 =
  `levelPct%`、`linear-gradient(90deg, --glow-500, --ice-500)`、`transition: width 80ms --ease-out`、
  `.is-speaking` 加 `0 0 6px rgba(247,150,255,.55)`）→ **slider**（轨道透明 4px pill +
  把手 14 圆 / `--indigo-700` / 2px `#fff` 边 / `margin-top:-5px` / `0 1px 4px rgba(70,91,146,.35)`）；
- **电平映射**：`levelPct = round(min(1, level^0.4) × 100)`（tsx 56–62，放大低音量：0.02→21、
  0.2→53、0.5→76、1→100）；**说话阈值 0.02**（tsx 134–135）；
- **slider 落地方式（用户拍板）**：用 Flutter `Slider` + **自绘 thumb**（`SliderComponentShape`），
  保住原生拖动/键盘/无障碍语义；`divisions: 100` 等价原生 `step=1`，轨道/overlay 全透明、
  `noTickMark`；`aria-label` 走 `MergeSemantics + Semantics(label:)` 与滑块自身节点合并。

#### 两处「逐行对齐但极易合并错」的语义（首版各错一处，测试抓到）

1. **`aria-pressed` 逐行不同**：自己行 = `micEnabled`（tsx 176，「是否开麦」）；远端行 =
   `locallyMuted`（tsx 187，「是否已静音」）——两处含义不同，**不能合并成一个 bool**；
2. **`.voice-muted-tag { color: --destructive }` 挂在 wrapper 上** ⇒ 「已静音」的**图标与文字**
   都取 destructive（图标 `currentColor`）。首版只给了图标色、文字仍是 `--text-secondary` ✗。

#### 复用与语义

`AvatarHalo`（size 32 / 在线光环 / 爱莉 `AvatarCore.elysia` / `onTap` / aria-label）·
`AylaIcon`（15 开关 / 11 静音标记）· `AylaPressScale`（按钮组 200ms + 1.02/.98 + 焦点环 + 语义）·
`MergeSemantics`（`aria-pressed` 与 `aria-label` 必须与框架自带语义节点**合并成同一节点**，
不能叠两层 `Semantics`）。

#### 测试侧两条新经验

- **`AnimatedContainer` 没有 `width`/`height` getter**（折进 `constraints`）——A5 已记过一次，
  本轮又踩 ⇒ 断言尺寸一律 `constraints.maxWidth/maxHeight`；
- **语义 dump 用 `containsKey` 判断「节点存在」**：若把 map 值当 toggled 标志，滑块这类非
  toggle 节点的值是 `null`，用 `isNotNull` 会误判成「标签不存在」。

（另：`previewScope` 的 `Overlay(initialEntries:)` 同一用例第二次 `pumpWidget` 不生效——本轮
第三次踩到，多状态用例一律树内 `StatefulBuilder`。）

#### 接线项 / 未完成项

页面层：presence 在线计算、用户缓存（昵称/头像 + `user_id` 前 6 位兜底）、`goUserProfile`、
`localAudioLevel` 数据源（承载层上报）；未完成项：两个 `title`（原生 tooltip）属全库
`Tooltip` 统一项（§4.2）。

#### 交付后用户实测报出的两条（2026-09-21 深夜，均已修 + 加像素级回归）

1. **「音量条两端不够圆角」**：量下来端帽**本来就是半圆**（DPR 1 与 1.25 下的覆盖率轮廓
   `4%/66%/95%/66%/4%`、`39/196/252/204/69` 都是透镜形）——因为 web 的 `border-radius: var(--radius-pill)`
   被浏览器夹到 `条高/2`，**4px 条上 R=2 已是几何上限**，再写大也无效。
   ⇒ 按用户指示「直接加大 R 角」：**条高 4 → 5、R 2 → 2.5**（记入 §6.3 校准值表，代码注释标「勿改回」）。
   同时把半径**显式写成 `barHeight / 2`**，不再依赖 `999` 被夹的隐式行为。
2. **「拖动滑块时后面的音量条有延迟」**：根因在 Flutter `Slider` ——
   **`divisions != null` ⇒ `isDiscrete` ⇒ `positionController.animateTo(convertedValue,
   curve: easeInOut)`**（`slider.dart:1292–1300`，`_positionAnimationDuration = 75ms`）
   ⇒ 拇指被**动画化、追不上指针**；而轨道是即时重建的 ⇒ 看起来「条子跟手、滑块拖沓」。
   web 的原生 `input[type=range]` 是跟手即时 ⇒ 去掉 `divisions`，`step=1` 的整数语义改在
   `onChanged` 里 `roundToDouble()` 表达。
   **回归测试用像素验证**：拖动后**只推进一帧**，拇指白环质心必须落在期望位置 ±2px
   （修前拇指还在动画中途 ⇒ 扫描不到白环）。

> **通用教训（已并入 11 号 §五）**：给 Flutter `Slider` 传 `divisions` 会把它变成「离散滑块」，
> 位置动画 75ms easeInOut——需要跟手就必须不传，整数步长自己取整。

**验收**：`dart analyze lib` 零告警；`voice_member_row_test` **24/24**（含 2 条像素级回归：
拖动跟手 / 端帽半圆）；全量 `flutter test --concurrency 1` → **397 通过 / 0 失败**。

### 6.24 B1-3 第一件：建语音频道表单（2026-09-21）

新文件 `widgets/voice_channel_create.dart`；事实源 `VoiceChannelCreate.tsx`（80 行全文）+
`app.css:2848–2869` + `auroraqua.css:502–531` + `private.css:229–236`；两个挂载点
（`ChannelSidebar.tsx:543` / `CreateFab.tsx:80`）**都在 `CreateSheet` 内** ⇒
private.css 的 sheet 作用域规则（输入 `width:100% + margin-bottom sp3`、`.btn-primary` 整宽居中）
**恒生效**，故 Flutter 侧按 Column + 整宽元素表达。

**两处实现口径（与 web 等价，测试已锁）**：

1. **64 字符上限用 `inputFormatters: [LengthLimitingTextInputFormatter(64)]`**，不用 `maxLength`
   —— 后者会让 Flutter 在字段下多渲染一个「0/64」计数器，web 没有这个元素；
2. **垂直居中靠 `padding: vertical 8` 补足**：web 是 flex `align-items: center` + `min-height: 36px`，
   Flutter 的 `Container` 没有该语义（子项贴顶）⇒ 13px × body 行高 1.55 ≈ 20 ⇒ 8+20+8 = 36。

**数据流边界**：web 在组件内直接 `voiceApi.createVoiceChannel` + `useVoiceStore.setChannels`
（插列表头、`mine:false`）；Flutter 侧组件只做「校验 / 防重入 / busy / 错误展示 / 成功后清空」，
请求与列表插入由页面层在 `onSubmit` 里完成（失败抛 `AylaVoiceChannelCreateException` 显示其文案，
其他异常显示「创建失败」），成功回调 `onCreated` 关浮层。群列表（web `useSocialPage('conversations')`）
经 `groups` / `groupsLoading` 注入。

**本轮踩的三个坑（都写进注释/本档）**：

1. **`GlassInput` 的参数名是 `semanticLabel`（单数）** —— 库内另有部件用 `semanticsLabel`，
   我按后者写导致 `undefined_named_parameter`；该报错**不告诉你是哪个构造器**，
   最快诊断是写一个最小复现文件对照（同参数在探针里通过 ⇒ 说明是拼写而非结构性错误）。
2. **`AylaVisibilitySelector` 的群搜索框也复用了 `GlassInput`**（`directory_controls.dart:970`），
   且**只在「指定群可见」勾选时渲染** ⇒ 测试里按类型取输入框会「时对时错」（群内创建的用例才炸）
   ⇒ 一律按 `hintText == '新语音频道名称'` 唯一锁定。
3. 选择器三个选项文案是 **「公开 / 好友可见 / 指定群可见」**（web `VisibilitySelector.tsx:62/66/75`），
   不是「好友」。

**验收**：`dart analyze lib` 零告警；`voice_channel_create_test` **15/15**；
全量 `flutter test --concurrency 1` → **395 通过 / 0 失败**。

### 6.25 Windows 端「进程活着、没有任何页面」的两个独立根因（2026-09-22，用户报「打不开 debug 应用」）

**现象**：Windows 侧跑 `flutter run -d windows`，构建成功（`√ Built …ayla_flutter.exe`），
却**没有任何窗口出现**；反复尝试会堆积出一串 `ayla_flutter.exe` 残留进程
（13 个，全部 `MainWindowHandle = 0`、`Responding = True`）。

**根因 A：把没有 `main()` 的样张库当 `-t` 入口。**
`lib/preview/component_gallery.dart` 是 `@Preview` 样张库，**没有 `main()`**，它由 widget-preview
宿主（`.widget_preview/`，入口 `generated_preview.dart` 自动生成）扫描渲染。
用 `flutter run -d windows -t lib/preview/component_gallery.dart` 时，Dart kernel 编译**不校验入口**
（所以构建成功），引擎加载才失败：

```
[ERROR:flutter/runtime/dart_isolate.cc(146)] Could not prepare isolate.
[ERROR:flutter/runtime/runtime_controller.cc(576)] Could not create root isolate.
```

⇒ **画布的 Windows 入口就是默认的 `lib/main.dart`**（`_UiStage` 用 `_showGallery` 在
登录页 ↔ `ComponentGallery` 间切，右下角小 FAB）。`-t` 在 Windows 端没有可用 target。

**根因 B：`main.dart` 在 binding 初始化前取 `SemanticsBinding.instance`。**
提交 `7179b29` 引入的 `lib/main.dart:34`：

```dart
void main() {
  if (kDebugMode) {
    SemanticsBinding.instance.ensureSemantics();   // ← binding 尚未初始化
  }
  ...
```

运行期抛 `Unhandled Exception: Binding has not yet been initialized`（`dart_vm_initializer.cc(40)`，
栈顶 `main (package:ayla_flutter/main.dart:34:22)`）⇒ `main()` 当场中断 ⇒ `runApp()` 永不执行
⇒ **没有首帧** ⇒ Flutter Windows 模板里窗口的显示挂在 `SetNextFrameCallback([&]{ this->Show(); })`
⇒ **Win32 窗口已创建却从不 Show**：`MainWindowHandle = 0`、`IsWindowVisible = false`、进程照旧存活。
**修法**：取值前先 `WidgetsFlutterBinding.ensureInitialized()`（幂等，`runApp` 内部还会再调）。
长期未被发现的原因：验收一直走 web 预览宿主，而**宿主不执行 `main.dart`**。

**诊断手法（可复用）**：

- 「进程活着 + 无窗口」先怀疑**Dart 侧没出首帧**，不是窗口系统/驱动问题；
- 抓运行期日志要**直接跑产物**：`build/windows/x64/runner/Debug/ayla_flutter.exe > log 2>&1`
  （`flutter run` 的终端输出会吞掉一部分栈）；
- 判定「窗口到底有没有」要枚举**全部**顶层窗口（含隐藏）：`EnumWindows` +
  `GetWindowThreadProcessId` 按 PID 过滤 + `IsWindowVisible` —— `Get-Process` 的
  `MainWindowHandle` **只反映可见主窗口**，全是 0 也可能是「窗口存在但没 Show」；
- 残留的 `ayla_flutter.exe` 建议清理后再验（本机实测残留**未**阻止后续构建成功，
  但会混淆窗口/进程判定）；若 `taskkill /F` 报「拒绝访问」，说明它以更高权限启动，
  需在管理员任务管理器里结束。

**已知非致命日志**（出现不必当 bug 查）：

```
[ERROR:flutter/impeller/entity/contents/contents.cc(119)] Break on 'impeller::ImpellerValidationBreak':
Contents::SetInheritedOpacity should never be called when Contents::CanAcceptOpacity returns false.
```

—— Impeller 的 debug 验证提示，窗口正常显示、交互正常。

**验收**：`dart analyze lib` → `No issues found!`；`flutter build windows --debug` 成功；
实跑产物后 `MainWindowHandle = 265302`、`MainWindowTitle = ayla_flutter`（窗口真实可见）。

### 6.25 B1-4：语音频道面板（2026-09-21）

新文件 `widgets/voice_channel_panel.dart`；事实源 `VoiceChannelPanel.tsx`（158 行全文）+
`app.css:2873–2915` + `voice.css:32–56 / 377–381` + `auroraqua.css:584–610`；唯一使用点
`VoiceRoomBody.tsx:297`。

#### 两个**独立**的上下文档（别合并）

| 档 | 事实源 | 效果 |
|---|---|---|
| `roomContext` | `voice.css:32–38 / 43–48`（`.voice-room-body .voice-panel`） | `width: 100%` + **`max-width: none`**（覆盖 app.css 的 560）、成员列表 `flex:1; min-height:0` **自己滚动**、其余子项不收缩 |
| `ownMaterial` | `auroraqua.css:584–610` | **宽屏**房间：面板透明（材质交给 `.voice-room-voice-card`）；**窄屏**：外层卡透明（材质归面板）⇒ 材质归属**按断点切换** |

两者正交（窄屏房间 = `roomContext: true` + `ownMaterial: true`）⇒ 两个参数，不合并。

#### 两条「web 无声明值」的等价说明（已在测试里锁住）

1. `.voice-owner-member-actions` 在 CSS 里**没有任何规则**，两个按钮的 4px 间距来自 JSX
   换行产生的空白 ⇒ Flutter 用 `Row(spacing: 4)`（`ownerActionGap`，注释登记）；
2. `h3` 的 `font-weight: bold` 未被 base.css 重置（它只重置 margin/padding）⇒ 标题是
   **16px / 700**，不是 400。

#### 测试侧两条新坑

- **`max-width: 560` 必须用宽松宽度宿主才量得出**：竖向 `SingleChildScrollView` 给的是**紧宽**
  约束，`min(700) > max(560)` 时 min 胜出 ⇒ 断言永远读到 700。用 `Align`/`Center` 包一层才有意义。
- **`roomContext`（含 `Expanded`）需要有限高宿主**：放进竖向滚动视图会直接抛
  `RenderFlex children have non-zero flex but incoming height constraints are unbounded`
  ⇒ 该档的用例用 `hostBounded`（`SizedBox` 固定高，无滚动视图）。

（另：本轮又一次被「同一用例里第二次 `pumpWidget` 不生效」（§6.19/§6.21/§6.23 同坑）绊到两次
⇒ 多状态/多档位一律拆用例。）

#### 数据流边界

面板内**只保留两条纯列表规则**：自己置顶兜底（tsx 65–72）、房主操作的 busy 管理（tsx 101–113，
含失败静默）；分页、成员事实与展示投影（昵称/头像/在线）、`voiceWS` 失效信号、API 调用
全部由页面层注入。

#### ⚠️ 我自己踩的坑（用户看预览当场指出）：样张里自造了「外层卡材质」

首版样张给房间上下文档套了一层 `ColoredBox(sakura-100 @ .35)` 当「模拟外层卡」，
在预览里就是一块**硬边的半透明粉矩形**（用户：「这个左边的卡片背景有问题」）。
回读 CSS 才发现：**`.voice-room-voice-card` 正常路径下没有任何材质声明**
（`voice.css:23–29` 只有 flex/overflow/margin；`auroraqua.css:538` 的 `--surface` 只在
`@supports not backdrop-filter` 兜底路径生效），而宽屏时它内部的 `.voice-panel` 也是透明的
（`auroraqua.css:594–602`）⇒ **宽屏房间整列直接浮在极光背景上**；窄屏才反过来
（外层卡透明 `584–589`、材质归 `.voice-panel`）。
⇒ 已删除该 mock，房间档就是「透明面板 + 固定高 + 列表自带滚动」；
**纪律**：样张只允许用 web 真实存在的材质（库内件或逐条推导），不得为了让画面「看起来像张卡」
自造背景层。

**验收**：`dart analyze lib` 零告警；`voice_channel_panel_test` **19/19**；
全量 `flutter test --concurrency 1` → **416 通过 / 0 失败**。

### 6.26 B1-5：爱莉语音面板（2026-09-21）

新文件 `widgets/elysia_voice_panel.dart`；事实源 `ElysiaVoicePanel.tsx`（108 行全文）+
`app.css:3122–3156 / 1324–1333 / 1362`。

#### ⚠️ 事实前提：web 里这个组件**没有挂载点**

`grep -rn "ElysiaVoicePanel" --include=*.tsx` **只命中它自己**（`useElysiaVoice` 也只有 vitest
测试）——「控制面闭环」只做到 hook，页面接线从未做 ⇒ **web 上无法对照观感**，样张是唯一视觉
验收面。已与用户确认：仍按 B1 清单复刻（样式自洽、库内件齐备，页面接线时可直接用）。

#### 两条与 B1-4 面板**极易抄错**的差异

1. `.elysia-voice-head` 是 **`align-items: center`**（B1-4 的 `.voice-panel-head` 是 `baseline`）；
2. 收起档 `.collapsed` 有自己的 **`padding: sp3`**（展开是 `sp4`）+ `align-items: flex-start`
   ⇒ 按钮贴左（按钮自身 `.btn` 的 24px 内边距不算在这条里）。

#### 行为口径（逐条对齐）

- 空文本**不受理则不清空**输入（web 的拦截在 hook 的 `sendText`）⇒ Flutter 侧
  `onSubmitText` 返回 `Future<bool>`（是否受理），面板据此清空；抛错同样保留输入；
- `maxLength={2000}` 用 `LengthLimitingTextInputFormatter` 表达（`maxLength` 会多出「0/2000」计数器）；
- `busy` 时**三个按钮一起**禁用（发送 / 结束通话 / 重新发起）；
- 终态（`isTerminal`）不渲染输入行，行动区换成「重新发起」；
- 面板内只持有 `open` / `text` 两个本地 UI 状态（web `useState`），通话编排全部注入。

**验收**：`dart analyze lib` 零告警；`elysia_voice_panel_test` **14/14**；
全量 `flutter test --concurrency 1` → **430 通过 / 0 失败**。

### 6.27 B1-6：语音房整页（voice 域 8/8 收官）（2026-09-21）

新文件 `widgets/voice_room_body.dart`；事实源 `VoiceRoomBody.tsx`（327 行全文）+
`voice.css:12–470` + `app.css:2105–2138 / 3473–3477` + `base.css:463–472`。

#### 三处实现要点

1. **材质归属按断点切换** ⇒ 用 builder 槽而不是透传参数：web 把 11 个 props 直传
   `VoiceChannelPanel`，而 Flutter 面板参数更多（成员/分页/音量回调…），若在房间体重复声明
   两套注入面会让 API 爆炸 ⇒ `voicePanelBuilder(bool ownMaterial)`：房间体只决定**材质档**
   （宽屏 `false`：材质在 `.voice-room-voice-card`；窄屏 `true`：外层透明、材质归 `.voice-panel`）。
2. **窄屏浮层用零高锚点表达 `position: absolute; bottom: 100%`**：
   `SizedBox(height: 0)` + `OverflowBox(alignment: bottomCenter, maxHeight: 300)`
   —— `Stack` 在竖向无界父级（Column）里会直接抛
   「A Stack requires bounded constraints from its parent」。
3. **「删除房间」= 无材质的裸 `.btn`**：tsx 用 `btn btn-danger`，但 `.btn-danger` 在整个 CSS
   **没有任何定义**（与 A5 的 `--glass-bg-hover` 同类）⇒ 用户 2026-09-21 裁决**按实渲染**复刻。

#### 未读规则留在面板内（纯 UI 逻辑，可测）

新 id + 聊天栏收起 + 非自己发送 ⇒ +1；展开即清零；`seenIds` 上限 1000；**初始装配的消息视为
已见**（历史首屏不产生未读）——逐条对齐 tsx 108–145。

#### 本轮抓到的真 bug 与测试坑

- **真 bug**：`channelId == null`（无聊天卡）时宽屏 `Row` 仍取 `children[1]` ⇒ `RangeError`
  （web 里第二列是空的）⇒ 已改为条件插入；
- **受控组件**：草稿变化必须 `setState`（否则发送钮的 `disabled` 不更新）——测试当场抓到；
- 又一次踩「同一用例第二次 `pumpWidget` 不生效」⇒ 新增 `driven()` 辅助（`StatefulBuilder`
  在同一棵树内 setState 驱动 props 变化），多状态/多断点一律拆用例。

#### 交付后用户实报的两条（2026-09-21，均已修）

用户看预览报：「聊天输入框遮罩把整个大画布都遮住了」「聊天输入框没有玻璃底」。
**同一个根因**：窄屏聊天气泡卡我手搓了裸 `BackdropFilter`（`BackdropFilter(child: card)`）
—— Flutter 的 `BackdropFilter` **未裁剪时过滤的是整个 backdrop**，于是把整块画布（含上一行的
宽屏块）都糊成涂抹带；同时卡面材质没走库内 `GlassSurface`，导致输入条浮在裸背景上（= 「没有玻璃底」）。
⇒ 窄屏聊天卡与窄屏浮层**都改用库内 `GlassSurface`**（它内部用 `ClipRect` 包住模糊层；
`strong: true` + 上两角 radius 16 + `0 -4px 16px rgba(70,91,146,.12)` 表达浮层）。

**取证方式（可复用）**：把样张 `RepaintBoundary.toImage()` 落成 PNG 再放大看 + 逐点取色 ——
在**纯白底**上模糊溢出看不出来，必须**铺渐变底**（同画布）才暴露；输入条填充与卡片底色
实测差 < 5/255（`--glass-bg` 叠两层在近白底上不可见）⇒ 用户看到的「没有玻璃底」本质是**卡面缺失**。

#### 第三条（库内件）：空 label 的图标钮不居中（2026-09-21 用户实报「这三个发送键好歪」）

用户截了三张图（发帖面板、房内聊天宽屏、房内聊天窄屏），红框都圈着同一类按钮。
根因在 **`GlassButton`**：内容布局是 `[Icon, SizedBox(gap 8), Text(label)]`，
`label: ''` 时那 8px 间隙仍在 ⇒ 内容整体偏左 4px。
**CSS 事实**：`.btn { gap: 8px }` 对**单个子元素**不产生任何间距（`gap` 只在两项以上生效）。
⇒ 修为：`label.isEmpty` 时不渲染 `Text`、也不加间隙；语义标签只在空 label 时取 `semanticLabel`
（不再写进空串）。**受影响调用点 3 处**（都是窄屏 `label: ''`）：`post_editor.dart:393`、
`comments.dart:441`、`voice_room_body.dart:592`。

新增库内回归 `test/glass_button_test.dart`（4 条）：图标+文字间距 8 且整体居中 /
空 label 图标严格居中（±0.5px）/ 语义标签取 semanticLabel / 禁用态布局不变。

**验收**：`dart analyze lib` 零告警；全量 `flutter test --concurrency 1` → **459 通过 / 0 失败**。

### 6.28 B2-1：弹幕三件（live 域第一批）（2026-09-22，待用户验收）

**交付**：`widgets/danmaku.dart`（三件 + 样张）、`widgets/danmaku_tracks.dart`（轨道纯函数）；
测试 4 份共 **54** 条（`danmaku_tracks_test` 8 / `danmaku_list_test` 19 / `danmaku_input_test` 16 /
`danmaku_overlay_test` 11）；画布加一节「live 域第一批」；基线 **459 → 514 全绿**。

**用户 2026-09-22 逐条拍板（勿改回）**：

1. **三件一批**（不是一件一件做）——DanmakuList + DanmakuInput + DanmakuOverlay 同轮交付；
2. ~~**`material` 三档枚举**表达 `.danmaku-wrap` 的上下文材质（`bare` / `swipeItem` / `swipeItemNarrow`）；
   studio side 档与 `bare` 实渲染等价 ⇒ 不设第三档~~ → **已作废（当时判断错误，勿再引用）**：
   我按 live.css 756–764 造的 `swipeItem`（玻璃 + `radius 0 0 12 12`）那条规则**没有任何渲染面**
   （窄屏专属容器 + live.css 818–839 媒体查询清零，见本节末尾「交付后用户裁决（第二条）」）
   ⇒ 最终**删除整个枚举**：组件根永不持材质，只留布局参数 `minHeight`；材质由调用方用库内卡片件给。
3. **查看器复用库内 `AylaImageViewer`**，但宿主改为**插 root Overlay**（`aylaOverlayEntry`）：
   web 的查看器是 `position: fixed; inset: 0`（全屏且不被祖先 `overflow:hidden` 裁），
   组件直接渲染会被播放器/列表卡裁掉 ⇒ 与「溢出宿主边界必须走 Overlay」同一条纪律；
4. **新增公共件 `AylaHistoryControlsData`**（放 `directory_controls.dart`，与控件同处）；
   voice 域既有的 `AylaVoiceChatHistory` 本轮**不动**（不回头改已验收代码）；
5. **图片失败态照实渲染**（同 A5 `--glass-bg-hover`、voice `.btn-danger` 口径）：web 里
   `.resource-image-failed-wrap` 的骨架（`fallback`）铺满 96×64，其后的「图片加载失败，点击重试」
   芯片被按钮 `overflow:hidden` **裁掉不可见**；`ResourceImage.tsx` 96–115 的 `enclosingControl`
   把按钮 click 抢过来重试 ⇒ **点击 = 重试，不开查看器**。Flutter 侧为此给共享件加
   `ResourceImage.onStateChanged`（additive，默认不变），并按 web 用 `ClipRect + OverflowBox` 复现裁剪；
6. **`GlassButton` 加一档 `glowBorderOnHover`**：`.danmaku-image-btn`（同时带 `btn btn-ghost`）的
   hover 是**三条规则各属性分别胜出**——底色 `.btn-ghost:hover:not(:disabled)` 冰蓝 `.18`（0-3-0）、
   阴影 auroraqua 按钮组 `button-hover`（0-3-0，压过本条的 glow-shadow 0-2-0）、
   边色 `.danmaku-image-btn:hover/:focus-within` `--glow-500`（0-2-0）。
   既有 `glowHover` 档是 `.post-editor-image-btn`（非 ghost）的形态 ⇒ 不能复用；
   **顺带修正**：`glowHover` 与新增档现在都覆盖 `:focus-within`（posts.css 410 / app.css 3787 都写了）；
7. **图片三步（选/传/发）由页面注入**（`onPickImage` / `onUploadImage` / `onSend`）：组件持 attempt
   （文件 + mediaId）⇒ 上传失败**重传同一份文件**、发送失败**复用 media_id**（web tsx 84–105 的重试语义）。
   为此在 `AylaMediaActions` 补两个一行组合 `pickImage()` / `uploadImage()`（页面接线一行）。

**关键事实（tsx 注释与 CSS 不一致，以 CSS 为准）**：飘弹幕关键帧终点是
`transform: translateX(calc(-100% - 24px))`（live.css 1006–1018），而 `DanmakuOverlay.tsx` 的注释
写的是「-100% = 自身宽度」——**多 24px 才完全离屏**，实现按 CSS。

**本轮新踩的坑（供后续 agent 复用）**：

1. **`Transform` 自身不随位移动**：`tester.getRect(fly(id))` 读到的是 `RenderTransform` 的**布局盒**
   （永远停在起点），位移要量**它内部的内容**（如 `find.descendant(of: fly(id), matching: find.byType(Row))`）。
   同理「飘完就移除」这类断言必须量内容的位置变化，否则会误判「没有动画」；
2. **非 `opaque` 的 `GestureDetector` 在空子树时命不中**：弹幕图片钮在加载/失败态下子树是骨架
   （空装饰盒不参与命中测试）⇒ 点击穿透到列表。web 的 `<button>` 整块可点 ⇒ 必须
   `behavior: HitTestBehavior.opaque`（两处：列表图片钮、飘弹幕图片钮）。这是**测试抓出来的真 bug**；
3. **`previewScope` 的 `Overlay(initialEntries:)` 只在首次创建生效**——「换 `ownerKey` 重置草稿」
   用例第二次 `pumpWidget` 完全没生效（`didUpdateWidget` 都没触发，插桩才确认）⇒ 一律用
   `StatefulBuilder` 在同一棵树内 setState 驱动（§6.19/6.21 已记过一次，本轮又踩）；
4. **测试视口必须显式钉死**：默认测试窗口是 800×600 **物理 / DPR 3**（逻辑仅 266×200），
   不设 `tester.view.physicalSize` + `devicePixelRatio = 1.0` 时 `SizedBox(420)` 会被父约束夹住、
   行左边界读数失真（实测读到 202 而非 12）；
5. **全屏浮层的宿主高度**：查看器插 root Overlay 后会占满**应用窗口**，测试里若把窗口做成
   640×200 的「视频区」尺寸，查看器自身会 `RenderFlex overflowed by 19px`
   ⇒ 宿主窗口与「视频区」尺寸要分开（本轮 `host(stage:, window:)`）。

**库内扩展（本批仅这 4 处，均 additive / 默认不变）**：`GlassButton.glowBorderOnHover`、
`ResourceImage.onStateChanged`（+ `AylaResourceImageState`）、`AylaHistoryControlsData`、
`AylaMediaActions.pickImage/uploadImage`。

**接线项（本件不做，属页面层）**：弹幕数据源（live store 投影）、在线态（presence）、
历史分页（`useCursorHistory` 等价物）、上传/发送注入、`channelKey` 的来源；
Tooltip 仍属 §4.2 全库缺口（`.danmaku-image-btn` 的 `title` 未复刻）。

#### 交付后用户实报的一条（2026-09-22，已修）

用户看画布样张报：**「窄屏这个输入框你又没加底」**（第二处同类：语音房窄屏聊天卡材质缺失，§6.27）。
根因不是漏抄 CSS，而是**材质所在的元素没并进来**：窄屏的玻璃底在 web 的**包装层**
`.live-room-input` 上（`live.css 768–772`：`--glass-bg` + `backdrop-filter: blur(18px) saturate(1.4)`，
无边/无阴影/无圆角），而我只给了 `base` 档（app.css 3764–3767：`padding sp3` + 上边框，**自身无底**）
⇒ 窄屏下整条输入浮在极光背景上。
⇒ `AylaDanmakuInputMaterial` 增第三档 **`narrowCard`**（玻璃底并入组件根：blur18 sat1.4 / 无边 / 无阴影 /
零圆角，内层仍是 `_bareArea` 的 `padding sp3` + 上边框）；样张改为
「**`sideCard`（≥769 直播侧栏卡内，见下方第三条裁决）** / **`narrowCard`（≤768 沉浸态）** /
base（studio 窄屏：`.live-room-side` 本身透明，auroraqua 569–581）」，并加定向测试锁住三档材质与内层 padding/边框。

**教训（写进 skill）**：复刻「上下文材质」时，**先确认材质挂在哪个元素**——
web 常见「包装层/祖先持材质、组件自身透明」，只抄组件自身的声明会漏底。

#### 交付后用户裁决（第二条，2026-09-22）：弹幕列表**不该自造材质**

用户看画布后原话：**「弹幕区玻璃背景看上去重复造轮子了，就是卡片而已，不存在上方角下圆角的，
窄屏更是弹幕区根本没有背景的」**。三条全部成立，我上一轮的 `material` 三档是**造轮子**：

| 我的实现 | 事实链（已逐条回读） |
|---|---|
| `swipeItem` 档 = `--glass-bg-strong` + blur18 sat1.4 + **只上边框** + **`radius 0 0 12 12`** + min-height 96（照 live.css 756–764） | ⚠️ **这条规则没有任何渲染面**：`.live-room-swipe-item` 只在窄屏分支渲染（`LiveRoomBody.tsx` 441 `if (isNarrow && !showOwnerPanel)` / 468），而 live.css **818–839 的 `@media (max-width: 768px)`** 对同一 `.danmaku-wrap` 把 `border-top: 0` / `background: transparent` / `backdrop-filter: none` / `border-radius: 0` **全部清零** ⇒ 「上直下圆的玻璃卡」在 web 里从不出现 |
| `swipeItemNarrow` 档 = 去材质 + min-height 96 | 半对：min-height 96 **是**真的（live.css 758 与 830 都声明），但它属于**布局**不是材质 |
| `bare` 档 = 无材质 | ✓ 对：app.css 3671–3676 只有布局；宽屏/控制台的弹幕列表在 `<aside className="live-room-side">` 里（tsx 531–534）⇒ **材质归那张卡片**（auroraqua 392–400：`margin 12` / 1px 边 / `radius 16` / `--glass-shadow` / blur24）——「就是卡片而已」 |

**改法**：删掉 `AylaDanmakuMaterial` 枚举与 `_wrapMaterial`；组件根**永不持材质**，
只留 `minHeight`（live.css 758/830 的 `min-height: 96`，窄屏滑动单元传 96）；
材质一律由调用方用**库内卡片件**给。样张重排为「宽屏（库内卡内，材质是卡片的）/ 窄屏（裸放，无底）/
空态+历史档」；测试改为锁「组件子树内无材质件 + minHeight 是布局档 + 宽屏材质来自祖先卡片」。

**教训（写进 skill）**：**先确认规则有没有渲染面** —— 同一元素在「只在窄屏出现的容器」里被
窄屏媒体查询清零时，那条基础规则就是死代码；只 grep 到它、没做「媒体查询叠加 + 分支可达性」
核验，就会造出一档永不见光的材质。

#### 交付后用户实报（voice 域 B1-6，2026-09-22）：聊天卡 head 又叠了一层底

用户看画布圈出语音房聊天卡顶部的「房内聊天 / 7 条消息」条：**「顺便修复一下语音房的这处造轮子」**。
根因与 §6.28 末尾那条同源（**只看组件自身声明、漏了 auroraqua 的清零**）：

```
voice.css 389–397   .voice-room-chat-card-head（@media ≥769）{ padding sp3 sp4 ·
                      border-bottom 1px --glass-border · background: var(--glass-bg) }  ← 我照抄的
auroraqua.css 593–599  @media (min-width: 769px)  同样断点、最后加载、特异性 0-2-0：
                      .voice-room-chat-card > .voice-room-chat-card-head
                      { background: transparent; box-shadow: none; backdrop-filter: none } ← 覆盖 ⇒ 实渲染透明
```
我照 voice.css 抄了 `--glass-bg`（字面量 `0x8CFFFAFB`）⇒ **卡面（GlassSurface 的 `--glass-bg`）上又叠一层**，
顶条比卡身更白一档 —— 用户一眼看出。**改**：head 底色改回透明，只保留下边框；顺带按 CSS 对齐
`gap: sp2` + `justify-content: space-between`（此前用 `Spacer` 顶替，视觉近似但语义不同）；
新增定向测试锁「透明底 + 仅下边框 + padding sp3 sp4 + gap sp2 + spaceBetween」。
（同批排查：composer 已是透明、窄屏卡片/宽屏卡片的材质归属正确、`.voice-room-head` 不在 auroraqua
402–405 的 `:is()` 列表内 ⇒ 无阴影是对的。）

#### 交付后用户裁决（第三条，2026-09-22）：宽屏输入条是**卡片内一栏**，不是独立浮卡

用户发来 web 端真机截图（宽屏直播侧栏）：**「宽屏直播界面的输入框不是独立的，是在卡片里的」**。
我上一轮实现的 `floatingCard` 档（照 auroraqua 347–359：`--glass-bg` + 1px 全边 + radius 16 +
`--glass-shadow` + blur24 + `margin 12` + `padding sp2`）**在真实直播房里永不渲染**——层叠实算：

```
auroraqua 347–359   :is(.composer, …, .danmaku-input-area)（@media ≥769，0-1-0）= 上面那套浮动卡材质
auroraqua 555–562   .live-room-body .live-room-side > .live-room-input > .danmaku-input-area（0-4-0）
                      background: transparent · box-shadow: none · backdrop-filter: none
auroraqua 563–567   同上：border-color: transparent · border-top-color: var(--glass-border) · border-radius: 0
⇒ 实渲染 = margin 12 + padding 8 + **透明底** + **仅上边框（卡内分隔线）** + 方角
```
（窄屏沉浸态没有 `.live-room-side` 祖先 ⇒ 347–359 不适用 ⇒ 区域保留 app.css 的 `padding sp3` + 上边框 ✓
`narrowCard` 本来是对的；studio 窄屏同样吃 555–567 但无 margin/padding 覆写 ⇒ 实渲染等价于 `base` 档。）

**改法**：`floatingCard` → **`sideCard`**（`margin 12` + `padding sp2` + 透明 + 仅上边框 + 方角，**无玻璃件**）；
`_bareArea` 加 `padding` 参数（基础档 sp3 / 侧栏档 sp2）；样张新增**真实装配格**
（`.live-room-side` 库内卡里 = 弹幕列表 + `sideCard` 输入条同卡，可交互发弹幕追加到列表）——
即用户截图那段效果；定向测试改为锁「无 GlassSurface + margin 12 + padding sp2 + 透明底 + 仅上边框 + 无圆角」。

### 6.29 B2-2：直播大厅卡 + 网格 + 三项跨域统一（2026-09-22，待用户验收）

**交付**：`widgets/live_hall.dart`（`AylaLiveChannelCard` / `AylaLiveHall` / `AylaLiveCardData` /
`aylaFormatViewerCount` / `aylaLiveViewerBadge`）；测试 `test/live_hall_test.dart` **32** 条；画布加一节。
基线 **515 → 560 全绿**。

**用户 2026-09-22 拍板（三条都是「统一」，勿改回）**：

1. **卡片默认内建收藏键**（原选项「页面注入槽」被否）：`AylaFavoriteButton(compact: true)`（32×32），
   落在封面右上，窄屏 inset 12 / 宽屏 `calc(sp4 + sp1)` = 20（与 LIVE 徽标顶线、人数角标右缘同线）。
   - 首轮我曾按「要转发键，都要」在**卡片上**也加了转发键（直播卡 + 语音卡），
     **随后被用户否决并撤回**：「语音列表卡片上不用显示分享键，直播也不用」⇒ 卡片只留收藏键；
     转发键只出现在**房头部**（web `LiveRoomBody.tsx` 330–339 / 语音房头部，B1-6 已有）；
   - 但用户先前的另一条仍然有效：**转发键尺寸与收藏键统一 32×32**（见第 3 条），
     房头部那两键与收藏键同尺寸。
2. **来源标签三域统一为一个共享件 `AylaSourceTag`**（语音卡 / 帖子卡 / 语音房头部已全部换用）：
   - 度量取 **live 徽章档**：`.live-badge`（app.css 3371–3377：`padding 2×sp2` / pill / 12 / `--font-utility`）
     + `.live-badge-source`（live.css 486–493：`--sakura-300` 底 + `--grape-700` 字 + `max-width 12ch`）；
     两处都**不声明** font-weight / letter-spacing / line-height ⇒ 继承 body（400 / 0 / 1.55）；
   - 被取代的三份旧规格：`.voice-source-tag`（Fredoka 11 / ls .8）、
     `.post-card-tag`（utility 11 / w600 / **ice-100 灰底**）、live 卡自身的 `.live-badge-source`（保持）；
   - ⚠️ **`.post-card-tag` 的灰色是 web 的 bug**（用户判为错误）：其 `color: var(--ice-600)`
     在 tokens.css **零定义**（同 `--glass-bg-hover` 那类）⇒ 声明整条作废、字色继承，实渲染即灰底。
     Flutter 侧**不复刻**，一律粉色；
   - 容器**不统一**（用户裁决）：live/语音卡 = `AylaScrollingTags` 横向滚动，帖子卡 = `flex-wrap` 换行平铺。
3. **转发键尺寸与收藏键统一为 32×32**（用户：「web 端收藏键转发键大小不一致，我认为是错误」）：
   `AylaShareButton` 默认档由 40 → **32**（图标 18 → 16；需要复刻 web `.icon-btn-40` 的地方显式传
   `size: 40`）；帖子卡底排的分享键从裸 `AylaIconButton(size: 40)` 换成 `AylaShareButton`（32）——
   于是「收藏 32 + 转发 32」在帖子卡、语音卡、直播卡、语音房头部四处一致。

**追加裁决（同日，用户看图后）**：**卡片必须等高**——「直播卡片有错位挤压，直播所有卡片高度要一致，
顺便审计语音列表卡片有没有锁定高度」。做法与审计结论：

| 位置 | web 事实 | Flutter 处理 |
|---|---|---|
| 直播大厅网格 | CSS grid 默认 `align-items: stretch` ⇒ **同行卡等高**（「无 meta 行」的卡被撑到与最高卡同高） | 卡片新增 `reserveMetaSpace`（`AylaLiveHall` 恒传 true）：meta 行**恒占位**且高度固定为 `max(13×1.4, 12×body 行高 + 2×2)`；单独使用（搜索/目录投影）默认 false，不预留（web 那些上下文没有同行拉平） |
| 语音列表卡（审计） | 同上（`.voice-hub .voice-channel-list` 是 `display:grid`） | 原有实现已**预留 owner 行**（`12 × 1.55`）✓；本次审计发现唯一漏洞 = **head 行高度随「有无收藏键」变**（32 vs 标签行 ~22.6）⇒ 给 head 行加 `min-height: 32`（`_headMinHeight`）⇒ 同行卡高度由构造决定 |
| 为何不用 `IntrinsicHeight` | — | 卡片内含 `LayoutBuilder`（来源标签的 `max-width:55%` 需要），而 **LayoutBuilder 不支持 intrinsics**（会抛 "does not support returning intrinsic dimensions"）⇒ 只能「预留行高」（与语音卡 owner 行同一先例） |
| 样张舞台 | — | 网格内容比舞台高 ⇒ 样张被挤（用户截图所见）⇒ 舞台高度按内容重算（1000×640 / 420×480） |

**顺带修掉的既有 bug（写新测试时暴露）**：`AylaVoiceCardData.visibilityLabels` 与
`AylaLiveCardData.visibilityLabels` 原先**无条件**调 `aylaVisibilityLabels`，
于是 visibility 缺失时会走函数自己的「群可见」兜底；而 web 的
`cardVisibilityLabels`（`components/cards/cardData.ts:14–16`）是
`item.visibility ? getVisibilityLabels(...) : []` ⇒ **缺失即空数组**（「群可见」兜底只服务于
`visibility === "group"` 但群名为空的旧数据）。两处都已按 web 修正，语音卡旧断言
「未知→群可见」改写为「未知→**无标签**」+ 新增 group-无群名→群可见 的用例。

**本轮复用**：`AylaCardInteraction`（卡片族悬停/按压）· `GlassSurface` · `AylaScrollingText` ·
`AylaScrollingTags` · `AylaSourceTag` · `AylaFavoriteButton` · `AylaShareButton` · `AylaRevealItem`
（stagger 50ms / cap 300）· `ResourceImage`（`alt: ''` 装饰图语义）· `aylaVisibilityLabels`。

**已知小差（记录待裁）**：web 网格是 CSS grid（同行 `align-items: stretch` ⇒ 同行卡等高），
Flutter 用 `Wrap` 表达「等宽列 + 行高随内容」；正常数据下卡片高度天然相等，只有
「既无主播名又无标签」的边角卡会比同行矮一截（web 会拉伸留白）。

**又一次踩到的坑**：`previewScope` 的 `Overlay(initialEntries:)` 只在首次创建生效 ⇒
**同一用例里连续 `pumpWidget` 换 props 完全不生效**（本轮「状态三档 / 人数三态 / 两键偏移 /
列数三档」四个用例都栽在这上面）⇒ 已全部改成「按用例注册的 for 循环」（每个 case 一个
`testWidgets`）或用 `StatefulBuilder`。

### 6.30 共享件修复：`AylaScrollingTags` 的接缝线（2026-09-22，用户实测两轮）

用户报：「渐隐的右边和下面有一条接缝线」（语音卡 → 直播卡 → 帖子卡都有）。两轮定位与修法：

| 轮次 | 我查到的 | 结论 / 修法 |
|---|---|---|
| 一 | `AylaScrollingTags` 用 `SizedBox(height: 40)` + `OverflowBox(min/maxHeight: 40)` 顶高度 ⇒ 组件高度恒为 **40（无界祖先）/ 父级可用高（有界祖先）**，而真标签只有 ~23 ⇒ 父级一矮（卡片 meta 行 / head 行）就把居中的标签**上下裁几像素**、圆角被切断 | 改用横向 **`SingleChildScrollView`（禁手势）**：子级拿无界宽（可超出、无 overflow 报错）、**自身高 = 内容高**、自带裁剪 ⇒ 高度不再被父级污染（实测 tags/mask/clip 三者同框 200×23，标签上下不再被裁） |
| 二 | 用户先给「遮罩往右 1px」，我做成整体平移 `bounds.shift(1,1)` → 用户否掉；再按「右侧遮罩加宽」做成右缘 +1px → **用户也否掉（「不行。刚刚这个也回退」）** | **两处偏离全部回退**：渐隐几何回到与 web 同构的 `createShader(bounds)`（`base.css 787–802`）。**结论：那条「接缝」不是渐隐几何的 1px 偏差造成的**——它在本轮之前的版本里就存在，平移/加宽都无效。<br>⚠️ 待查方向（尚未定论，勿再盲改几何）：① 溢出判定阈值 `d > 1` 会让「亚像素溢出」既不滚动也不套渐隐 ⇒ 内容在容器边缘被硬切（阈值来自 B2 原始实现，对齐 web 的整数 `scrollWidth > clientWidth` 判定）；② 标签自身 `max-width: 12ch` 的胶囊右端帽被容器裁掉（web 同样会裁，但落在全透明区）；③ 标签条可用宽度（live 卡 `max-width: 55%`）偏紧 |

**纪律补充**：桌面端 `Scrollable` 会自动挂 `Scrollbar`（其拇指是一条灰竖线）—— 本组件这层滚动已用
`ScrollConfiguration(scrollbars: false)` 关掉（库内 `server_rail` 同款先例）。**任何「为裁剪/横向溢出而引入的
滚动容器」都要显式关滚动条**，否则会在边缘画出多余的线。

新增回归 `test/scrolling_tags_test.dart`（6 条）：组件高 == 内容高（两种父级高度下）· mask 覆盖范围 == 组件范围 ·
未溢出不加 mask · 不可拖（`NeverScrollableScrollPhysics` + `Clip.hardEdge`）· `AylaSourceTag` 统一档度量。

#### 6.30 补：最终落点（用户逐轮校准，2026-09-22）

用户第四轮给出确切操作：**「内部标签容器宽度收窄右侧一点点，渐隐遮罩完全不动」**。最终实现：
`AylaScrollingTags` 内层 **滚动视口右侧收窄 `_innerRightInset = 1px`**（命名常量、便于继续调数值），
`ShaderMask` 的几何**一行未动**（仍以 LayoutBuilder 尺寸为基准）。

本轮同时**回退**的用户否决项（都留在注释里，勿再改回去）：
- ❌ 渐隐整体平移 `bounds.shift(1,1)`；❌ 渐隐右缘 `+1`（两种 1px 微调对那条接缝无效）；
- ❌ 标签按可用宽均分收窄（**收窄后内容不再溢出 ⇒ 滚动/marquee 与渐隐一起失效**，
  用户实测立刻发现「怎么直接不滚动了」⇒ 回退）。

保留的真修复（与那条线无关，但都是共享件里的实际问题）：
- ✅ 组件高度 = 内容高（去掉 `SizedBox(height: 40)` + `OverflowBox` 的魔法高度）；
- ✅ 关掉滚动条（`ScrollConfiguration(scrollbars: false)`，桌面端否则会多一条灰竖线）。

### 6.30 B2-3：直播侧栏 + 开播选择器（2026-09-22，待用户验收）

**交付**：`widgets/live_rail.dart`（`AylaLiveChannelRail` / `AylaLiveStartSheet`）、
共享件 `widgets/dashed_border.dart`（`AylaDashedBorder`）；测试 `test/live_rail_test.dart` **23** 条；
画布加一节。基线 **554 → 587 全绿**（+23 本件、+4 弹窗组合与限高、+6 用户新增的 `scrolling_tags_test`）。

**用户 2026-09-22 拍板**：两件一批（Rail + LiveStartSheet）· 高亮走**容器级单实例 + 跨项迁移** ·
自动滚动用「就近」语义 · 窄屏覆盖层的**遮罩与定位留给 LiveRoomBody**。

**复用与新增**：
- 复用 `AylaNavHighlight`（**裸变体**——不是 A1 服务器列的 `--rail` 竖条）、`AylaIconButton(size: 36)`、
  `GlassSurface`、`ResourceImage`（装饰图 `alt:''` + `fallback`）、`AylaRevealItem`、
  `AylaDirectoryLoadMore`（`directoryFooter` 槽）、`aylaFormatViewerCount` / `aylaLiveViewerBadge`（B2-2 已入库）、
  `GlassButton(variant: glow/ghost)`；
- **新共享件 `AylaDashedBorder`**：由 `channel_sidebar.dart` 的私有 `_DashedBorderPainter` 提升而来
  （直播侧栏「新建直播间」`border: 1px dashed var(--ice-500)` 与频道侧栏「更多」键同一个绘制需求）；
  `channel_sidebar` 已改用它（42 条测试仍绿）。

**本轮新踩的坑（3 条，已写进 skill）**：

1. **CSS 的 `width` 压不过「紧约束父级」，但 Flutter 的 `SizedBox(width:)` 也压不过** ——
   `SizedBox` 内部是 `constraints.enforce(...)`：父级给 tight 宽度时会把 240 夹回父级值
   （实测放进 420 宽的紧宿主里变成 **396**）。要表达 CSS 的「宽度权威」必须
   `UnconstrainedBox(constrainedAxis: Axis.vertical)` 松掉**横向**紧约束（竖向保留 ⇒ 等价 web flex 行
   默认 `align-items: stretch` 拉满高度）。
2. **CSS `scrollIntoView({ block: "nearest" })` 没有 Flutter 等价**：`ensureVisible` 的
   `keepVisibleAtEnd/Start` 都是**单向**（只在超出对应那侧才动），`explicit` 又会无条件贴边、
   **打断用户手动滚动** ⇒ 显式实现「已可见不动 / 上方顶对齐 / 下方底对齐」；
   ⚠️ 测量必须**视口相对**（内容坐标系含列表 padding，直接用会让末项露头 1px），
   并守 `hasViewportDimension` / `hasPixels` 的首帧陷阱。
3. **「切换类」动画的目标几何在切换时不变化 ⇒ 要在 `didUpdateWidget` 当帧直接赋值**（build 紧随其后、
   无需 setState）。只靠 postFrame 重测会白等一帧才开启动画（探针实测：+400ms 未动、+800ms 才到位）。

**另两处事实（供后人免得重查）**：
- `.live-rail-float`（live.css 426–448「收起态浮动按钮」）是**死 CSS**：tsx 76–79 收起时直接 `return null`，
  返回/展开键由 `LiveRoomBody` 的顶栏承载 ⇒ **不复刻**；
- **用户本人的改动**（`primitives.dart` 的 `AylaScrollingTags` 遮罩/接缝修复 + 新增
  `test/scrolling_tags_test.dart`）**勿动勿回退**：其根因是旧实现用 `SizedBox(height:40)+OverflowBox`
  顶高度 ⇒ 卡片里（meta 行 22.6 / head 行 32）把标签圆角上下裁断，且桌面端 `Scrollable` 自动挂的
  `Scrollbar` 拇指被画成一条灰竖线（= 用户看到的「接缝」）；新实现改为横向
  `SingleChildScrollView`（高度=内容、自带裁剪、关滚动条）+ 内层右收窄 1px，渐隐几何未动。

#### 交付后用户实报（B2-3 追加，2026-09-22）：开播弹窗外层出现了滚动条

用户看画布报：**「你这弹窗布局有问题啊，外层不应该出现滚动条，应该在那个高度内正常自适应铺满」**，
随后自己定位到根因：**「是因为你多加了一行字导致的滚动条，本来不应该会滚动的」
「"已选择：准备中的第二场直播"这个得移出去」**。

- **根因**：我在 `_StartSheetDemo` 里把演示状态行（「已选择：…」）放进了 `AylaCreateSheet` 的卡内 ⇒
  内容被顶过 `.create-sheet-card { max-height: 80vh }` ⇒ 触发外层卡 `overflow-y: auto` 滚动 +
  桌面端自动 `Scrollbar`（卡右缘那条竖线）。**弹窗本身没问题**（A3 的限高与窄屏档都对：探针实测
  900×560 舞台下卡 480×355、`maxScrollExtent = 0`）。
- **改**：把演示状态行移到弹窗**之外**（样张里挂在弹窗外层 Column 底部）。

**⚠️ 纪律（我本轮越界，用户当场叫停）**：我还顺手做了一次「全库隐藏原生滚动条」的改动
（`AylaModalCard` / 弹幕列表 / 直播侧栏与开播列表各加 `ScrollConfiguration(scrollbars: false)`），
理由是 web `base.css:372–383` 全局隐藏原生滚动条。用户明确否决：
**「没叫你隐藏滚动条……去掉这行字自然就行了，回退对滚动条的修改」** ⇒ **已全部回退**
（三处文件 `git diff` 已确认干净）。
**教训**：用户点名的是**一个具体现象**（那行字导致的滚动条），不是「滚动条策略」；
把具体修复推广成跨组件一致性改动之前**必须先问**。原生滚动条隐藏属既有 §C/§B6 事项
（`OverlayScrollbar` 自绘覆盖层条待做；`server_rail`/`channel_sidebar` 两处历史改动保留原样），
**不要顺手全库改**。

### 6.31 B2-4：观看条 + 名单弹层 + 主播头像 + 推流地址（2026-09-22，待用户验收）

**用户拍板**：四件一批 · 名单弹层「head 固定 + body 内滚」· 弹层**插 root Overlay** · 数据/回调**页面注入**。

**交付**：`widgets/live_viewers.dart`（`AylaLiveViewerStrip` / `AylaLiveViewerSheet` / `AylaLiveViewerItem` /
`AylaLiveViewerSheetData`）、`widgets/live_studio.dart`（`AylaLiveHostAvatar` / `AylaLiveStreamAddresses` /
`obsServerFromRtmpUrl`）；测试 `test/live_viewers_test.dart` **16** + `test/live_studio_test.dart` **10**；
画布两节。基线 **587 → 613 全绿**。

**复用（一件都没新写材质/交互）**：`AylaCreateSheet`（弹层配方 + `narrowHeightFactor: 0.6` = 60vh）、
`AylaCardInteraction(interactive: false, focusRingColor: --focus-ring)`（自绘按钮的焦点环 + Enter/Space 可达）、
`AvatarHalo`、`AylaMsgActionButton`（复制键）、`AylaGlassInset.over`（值行的 `--glass-inset` 内高光）、
`AylaSkeleton`、`GlassButton(ghost)`、`aylaOverlayEntry`（root Overlay 宿主，同弹幕查看器先例）。

**本轮新踩的坑（3 条，已写进 skill）**：
1. **`ConstrainedBox(maxWidth:)` 与 `SizedBox(width:)` 同病**：都走 `constraints.enforce`，父级给紧约束时
   上限被夹回（实测 `min(100%,960px)` 的推流卡在 1200 宽宿主里变成 1200）⇒ 先 `Align` 松横向紧约束；
2. **一个用例里第二次 `pumpWidget` 换 props 不生效**（`previewScope` 的 Overlay 只在首次创建生效；
   本轮实测第二次 pump 连 `didUpdateWidget` 都不触发）⇒ 换 props/断点的用例一律用树内 `StatefulBuilder`；
3. **「同档还有一条把宽度压到 0 的规则」⇒ `flex-wrap` 是死声明**：`.live-copy-row` 窄屏档的
   `flex-wrap: wrap` 被同档 `.live-copy-value { min-width: 0 }` 顶掉，永不换行 ⇒ 只表达交叉轴对齐。

**顺带纠正的旧结论**：skill 里「用户最终修法是遮罩往右 1px（`bounds.shift`）」**已被用户最新实现推翻**——
现行为「**内层标签容器右侧收窄 1px**（`_innerRightInset = 1`），渐隐几何一行未动」，两种偏离均已回退；
skill 与本节已同步改正（避免后人按旧结论把 shader 又改回去）。

**live 域进度**：**11/16**；余 5 件 = `LiveCreate`(189) → `LiveOwnerPanel`(217) → `LiveMiniPlayer`(228)
→ `LivePlayer`(420) → `LiveRoomBody`(566)。

### 6.32 B2-5：建播表单 + 控制台资料栏（2026-09-22，待用户验收）

**用户拍板**：两件一批 · 封面预览**按 web 本意用 `cover`**（web 的 img 漏类名 ⇒ 死规则，视为 bug）·
复制行**抽共享件** · 字段件**内联**（不新造跨组件件）· 另：web 的**裸 `.btn`**（下播键无底无边无阴影）
**判为 web 的 bug**，用库内 `ghost` 档给回玻璃面（**有意偏离**，已登记）。

**交付**：`widgets/live_create.dart`（`AylaLiveCreate` + `AylaLiveCreateRequest`）、
`widgets/live_owner_panel.dart`（`AylaLiveOwnerPanel` + `AylaLiveOwnerSaveRequest`）、
`widgets/live_channel_snapshot.dart`（共享数据类 `AylaLiveChannelSnapshot`）、
**共享复制行** `AylaLiveCopyRow`（从 `live_studio.dart` 的推流卡抽出，两档：`base` = `.live-create-guide`
的 `--ice-100` + radius-sm 8 / `studioStream` = 卡内覆写玻璃底 + radius-input + 内高光）；
测试 `test/live_forms_test.dart` **14** 条；画布两节。基线 **613 → 627 全绿**。

**关键事实（登记）**：
- ⚠️ **`LiveCreate` 在 web 里零挂载点**（全仓 `<LiveCreate>` 零命中、vitest 无用例）：真实建播走
  `ChannelSidebar.handleCreateNewLive` → `createLiveChannel("新直播间")` 后直接进控制台
  ⇒ 与 B1-4 `ElysiaVoicePanel` 同款**孤儿件，组件画布是它唯一的视觉验收面**；
- `LiveOwnerPanel` 是**真实挂载**的（`LiveRoomBody` 的 `showOwnerPanel`）；
- app.css 的 `.live-create-input:focus { box-shadow: var(--focus-ring) }` 是**无效声明**
  （`2px solid #f796ff` 里的 `solid` 在 box-shadow 中非法）⇒ 生效的是 auroraqua 513–517；
- `.live-owner-panel` 的 padding 被 live.css 168 覆写为 **sp3**（app.css 3831 写的是 sp4）；
- 断点三档：≤768 与 **769–1100**（侧栏压缩控制台余宽）都走换行布局。

**本轮新踩的坑（3 条，已写进 skill）**：
1. **`Semantics(child: Expanded(...))` 会触发 ParentDataWidget 断言** —— `Expanded` 必须**直接**
   在 Flex 下（测试抓出来的真 bug：`_input` 里把 Semantics 包在 Expanded 外面）；
2. **响应式「换行档」里不能用 `Expanded`**：字段改列（纵向无界）会报
   「non-zero flex but incoming height constraints are unbounded」；`Wrap` 也给子级**无界宽度**
   ⇒ 换行档改用**确定性两行结构**（封面+字段一行 / 按钮独占一行），语义与 web 的
   `flex-wrap + flex-basis: 100%` 一致；
3. **`ConstrainedBox(minWidth: 96)` 下按钮的 `expand`（width:100%）会拿到无界宽度**并报 RenderFlex 断言
   ⇒ 用固定 96 表达（web 是 `min-width`，两个键文案固定 ⇒ 等价）。

**live 域进度**：**13/16**；余 3 件 = `LiveMiniPlayer`(228) → `LivePlayer`(420) → `LiveRoomBody`(566)。

#### 交付后用户实报（B2-5 追加，2026-09-22）：开播/保存按钮独占一行、太大

用户看画布原话：**「开播、保存按钮不要独占一行，太大太难看」**。

- **根因（照实照抄 web 的后果）**：web 在 ≤768 与 **769–1100** 两档把 `.live-owner-start` 设成
  `flex-basis: 100%`（**另起一整行**）+ 主键 `flex: 1`（横向**铺满**）—— 我照抄了这两档，
  而样张舞台恰好落在 900 宽的**中屏档**，于是整块铺满一行，视觉上又大又空。
- **改（用户裁决优先于 web）**：Flutter 侧**三档同构** —— 封面 + 字段 + 按钮块**始终同一行**，
  按钮块恒为 **96 宽**（`.live-owner-start { min-width: 96px }` 的实值）+ `min-height 40` 竖排
  （开播/下播 + 保存）；只有**字段**按断点改列（web 的窄/中屏字段 `width: 100%; flex: none` 保留）。
  登记为**有意偏离 web**（连同「裸 `.btn` 当 web 的 bug」一起写在 `live_owner_panel.dart` 的注释里）。
- 样张同步：新增一格 **>1100 的真宽屏档**（1240），中屏档（900）与窄屏档（420）各留一格。

#### 交付后用户实报（B2-5 再追加，2026-09-22）：按钮独占一行的后续 + 字段加高

用户第二条原话：**「保存按钮改宽一些。（仅宽屏的）输入框改高一些，对齐」**。

- **保存键**：用户截图里它是窄胶囊（~50 宽），根因是**上一版**在它外面套了
  `Align(alignment: center)` ⇒ 在 96 宽的列里被居中成内容宽。**上一轮已随「三档同构」一起删掉**
  （现在 = 96 宽，探针实测 `保存 btn = 1131→1227`）。
- **输入框（仅宽屏）**：web 是 `.live-title-input` / `.live-desc-input { min-height: 32px }`，
  用户要求加高并对齐 ⇒ **宽屏档字段固定高 40**（与开播键 `.btn min-height: 40` 同高、
  两个输入顶/底对齐、内容 `TextAlignVertical.center`）；**窄/中屏档保持 web 的 32**
  （用户明确「仅宽屏的」）。常量 `_wideFieldHeight = 40` 写在 `live_owner_panel.dart` 并注明
  「用户校准」——按项目纪律，实测校准值必须进代码注释。
- 测试相应加断言：宽屏 `GlassButton`/`AylaMsgActionButton` 同宽 96、两个 `TextField` 高 40 且顶底对齐；
  窄屏字段仍 32。

### 6.33 全库统一项：禁用态改为「按颜色降透明」（2026-09-22，用户批准）

**起因**：用户热重启预览后贴出控制台日志，其中反复出现
```
[ERROR:flutter/impeller/entity/contents/contents.cc(119)] ImpellerValidationBreak:
Contents::SetInheritedOpacity should never be called when Contents::CanAcceptOpacity returns false.
```
**根因（读代码确认）**：`base.css button:disabled { opacity: .55 }` 在库内实现为**整层 `Opacity(.55)`**，
而这些件的盒子里含 `BackdropFilter`（ghost blur8 / corner-fab blur18）：

| 件 | 位置 | 触发条件 |
|---|---|---|
| `GlassButton` | `glass.dart`（禁用分支） | `onPressed == null` |
| `AylaIconButton` | `buttons.dart` | 同上（如弹窗 busy 的关闭键、直播侧栏未传返回键） |
| `AylaCornerFab` | `buttons.dart` | 同上 |
| `AylaToolButton` | `buttons.dart` | 同上 |
| `AylaMsgActionButton` | `buttons.dart` | 同上，但其盒内**无** `BackdropFilter` ⇒ 不触发（另一个小差异：web 该件的 `blur(8px)` 本库未实现，登记） |

引擎层拒绝 `Opacity` 叠 `BackdropFilter` 的后果是**禁用态的 `.55` 变暗不生效**（不只是日志噪音）。

**用户裁决**：先只批 `GlassButton`（glass.dart），随后批准把同一机制推广到 `buttons.dart` 的三处。

**改法**：`.55` 落到**颜色**上（底/渐变/边/阴影各 ×.55；内容层单独 `Opacity`，因为图标/文字层不含
backdrop-filter ⇒ Impeller 安全），并**移除整层 `Opacity`**。首个实现在 `glass.dart` 的 `GlassButton`
（含 `dimColor` / `dimGradient` / `dimShadows` 三个局部助手），`buttons.dart` 用文件级
`_dimDisabled` / `_dimDisabledShadows` 跟随（注释里写明同一裁决与原因）。

**测试**：新增 `test/button_disabled_test.dart` **4** 条 —— 禁用 → 底色/边 ×.55；可用 → 原值；
以及回归点「`BackdropFilter` 的祖先里**没有** `Opacity`」（正是引擎拒绝的组合）；
另修 `voice_channels_test` 的离开键用例（原来用的是禁用态，改为传 `onLeave` 测可用态规格）。
基线 **627 → 631 全绿**。

**日志里另外两段的判定（与本批代码无关，登记以便不复查）**：
1. `services/raw_keyboard.dart:863` 的 Alt 断言 = Windows runner 在**热重启瞬间**送来 `modifiers: 0`
   的修饰键 key-down（重启前松开修饰键即可）；
2. `Unable to parse JSON message: The document is empty.` = `flutter widget-preview` 宿主在热重启瞬间
   收到空消息帧（SDK 源码内搜不到该字符串：打包在 `flutter_tools` snapshot 内）。

#### 交付后用户实报（B2-5 三追加，2026-09-22）：保存键「槽位 96 但胶囊仍 42」

用户第三条（附画了目标宽度的截图）：**「保存按钮改宽一些。……你这一步怎么没改？看不出来啊，
我给你画了改成多大。要对齐」**。

- **上一次改的是槽位、不是胶囊**：探针实测（1240 宽档）
  ```
  开播 slot=1131→1227 (96)  pill=1131→1227 (96)   ✅
  保存 slot=1131→1227 (96)  pill=1131→1173 (42)   ❌ ← 用户看到的窄胶囊
  ```
  **根因**：`AylaMsgActionButton` 的阴影环用 `Stack`（`fit: loose`）⇒ 会把约束**放宽**给面层，
  外层的 `SizedBox(width: 96)`（`Column(crossAxisAlignment: stretch)`）拉不宽胶囊。
- **改**：给 `AylaMsgActionButton` 加**纯新增档** `minWidth`（默认 null = 按内容自适应，
  现有调用方零影响），把最小宽**透传到面层** `AnimatedContainer` 的 constraints；
  面板里传 `minWidth: 96` ⇒ 胶囊与「开播」同宽对齐（实测 pill=1131→1227 ✓）。
- 测试相应把断言从「槽位宽」改成「**面层（AnimatedContainer）宽**」——只看槽位会被这个问题骗过。
- 输入框：宽屏档先取 40，用户随后在画布上比对**封面缩略图高度**后**定稿 54 = 封面高**
  （96 × 16:9 = 54）⇒ 整行「封面 / 直播标题 / 介绍」三者齐平（`_wideFieldHeight = 54`，
  常量旁注明两轮校准过程）；窄/中屏仍按 web 的 `min-height: 32px`（用户明确「仅宽屏的」）。
  测试断言：宽屏 `TextField` 高 54 = 封面高、两输入顶/底对齐且与封面同心。

**live 域进度不变**：13/16（余 `LiveMiniPlayer` → `LivePlayer` → `LiveRoomBody`）。
基线仍 **631 全绿**。

#### 交付后用户逐轮校准（B2-5 四追加，2026-09-22）：控制台资料栏的最终版式

用户在画布上逐轮画框定稿（每次只改一处），**最终形态**（宽/中屏）：

| 元素 | 最终尺寸 | 说明 |
|---|---|---|
| 行高 | **112** | 整行高度基准 |
| 封面 | **199.1 × 112** | 按 16:9 从行高反推（"等比例拉，加宽"） |
| 标题 | **200 × 48** | 单行；`contentPadding` 垂直按 `(48 − 22.5)/2` **反算**使填充块 = 盒子高 |
| 开播 | **200 × 60** | = 112 − 48 − sp1；与标题同宽同左/右缘，**下沿齐平** |
| 介绍 | flex × **112** | 多行可换行，用 **`expands: true` + maxLines/minLines null**（高度由父级决定，自适应） |
| 保存 | 96 × 112 | 独占右列并铺满（`AylaMsgActionButton` 的 `minWidth/minHeight` 档） |

窄屏（≤768）**布局不变**、三列**下沿对齐**：行高 72（= 字段列自然高 32+sp2+32），
封面 128×72（16:9 反推），按钮列 96×72 用 `spaceBetween` 吸收差值。

**三条硬经验（本项目反复踩，已进 skill）**：
1. **「盒子高 ≠ 可见填充块高」**：`SizedBox(height:)` 只改盒子，单行 `TextField` 的
   `InputDecorator` 按「内容+内沿」自算高并**顶部收起**（用户连续两次截图说「输入框没加高」）
   ⇒ 单行按目标高度反算 `contentPadding`；多行用 `expands: true`；
2. **`AylaMsgActionButton` 的阴影环 `Stack` 会放宽约束** ⇒ 外层 `SizedBox` 拉不宽/拉不高胶囊，
   尺寸必须**透传到面层**（本轮为此加了 `minWidth`/`minHeight` 档，纯新增、默认不变）；
3. **尺寸断言要量面层**（`AnimatedContainer`/`InputDecorator`），量外层 widget 会读到"槽位"而骗过测试。
